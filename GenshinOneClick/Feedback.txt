项目仓库：https://github.com/AizawaHikaru233/genshin_fsr_brigde

日志与问题反馈

Bridge、OptiScaler 和插件启动组件默认会保留错误日志。每次重新运行会覆盖上一轮日志。
遇到游戏无法启动、FSR 无法激活、切换超分后闪退或其他异常时，请在复现后不要再次启动游戏，并提供：

1. payload/Bridge/Dx11FsrBridge.log (必须)
2. payload/OptiScaler/OptiScaler.log (必须)
3. payload/OptiScaler/OptiScaler.ini
4. payload/ReShade/ReShade.log（涉及 ReShade 时）
5. 芙芙插件目录下的 FSR-Bridge-Plugin.log（使用芙芙启动器插件时）
6. 显卡型号、游戏版本、异常发生阶段和所选超分模式

需要进一步排查时，可临时将 OptiScaler.ini 中 [Log] 下的 LogLevel 改为 1（Debug）或 0（Trace），但诊断结束后应恢复正式配置以避免额外开销。
不要把游戏账号、登录信息或包含个人信息的截图提交到公开 Issue。


If the game cannot start, FSR does not activate, changing upscaling causes a crash, or another issue occurs:

1. Reproduce the issue, then do not launch the game again.
2. Submit `payload\Bridge\Dx11FsrBridge.log`.
3. Submit `payload\OptiScaler\OptiScaler.log`.
4. For ReShade/HDR issues, submit `payload\ReShade\ReShade.log`.
5. When using the FuFu Launcher plugin, also submit `FSR-Bridge-Plugin.log` from the plugin directory.
6. Submit `payload\OptiScaler\OptiScaler.ini`.
7. Describe the GPU model, the stage at which the issue occurred, and the selected upscaling mode.

Bridge and OptiScaler release builds keep basic errors by default and overwrite the previous run at startup, with no per-session size limit. ReShade 6.7.3 cannot filter logs by level, so `ReShade.log` contains the complete current session. For more OptiScaler detail, temporarily set `[Log] LogLevel` in `OptiScaler.ini` to `1` (Debug) or `0` (Trace).
