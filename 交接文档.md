# FSR Bridge 原神替换项目 — 交接文档

## 2026-08-26 第三十五轮：开放 SDK host 切换

- 已将 `Fsr2Sdk234Backend` 从动态 `ffx-api` / `amd_fidelityfx_upscaler_dx12.dll` 切换为静态链接的开放 FidelityFX FSR2 DX12 host：`ffxGetInterfaceDX12` → 每实例 `ffxFsr2ContextCreate` → `ffxFsr2ContextDispatch`。原有 D3D11 staging、motion 平方解码、输出 readback 落地、每实例历史隔离保持不变。
- CMake 已加入 SDK 的 `ffx_fsr2_x64` 与 `ffx_backend_dx12_x64` targets，并统一为 `/MD`；生成的 DLL 不再依赖 `amd_fidelityfx_upscaler_dx12.dll`。
- 已修正本机 SDK shader-CMake 的一个变量解引用错误（`sdk/include/FidelityFX/gpu/CMakeCompileShaders.txt`），否则 shader 目标把 `FSR2_PERMUTATION_OUTPUTS` 当成字面文件依赖。
- 已完成 Ninja Release 构建：`D:\FSR\build-open-sdk-ninja-release2\Dx11FsrBridge.dll`，SHA256=`2E29F23C433F7E7527F463666106E4667E78B4BC9B4BF1312595987B7E78BD16`；尚未部署或进游戏实测。
- 注意：本机开放 SDK 的 host 头版本为 **FSR2 2.3.2**，不是旧 runtime provider 的 2.3.4。此轮旨在先排除闭源 runtime/历史资源实现；游戏画质验证通过后再决定是否升级 SDK checkout。

- 编制：2026-08-26（第三十四轮后）
- 目标读者：接手本项目的开发者 / 后续会话
- 项目性质：原神 7.0（YuanShen.exe，D3D11 + il2cpp）内嵌 FSR2 的**外部替换**——hook 游戏调用 FSR2 的位置，改为调用 FSR2 2.3.4 SDK（经 OptiScaler ffxApi runtime `amd_fidelityfx_upscaler_dx12.dll`），目标是"真的替换掉"（非平行 skip+CopyResource 路径）。

---

## 1. 当前状态总览（一句话）

**画面已从"无 AA（稳定单帧）"突破为"累积存活但全画面涂抹/油画感 + 破碎拖影 + 锯齿不退"**——motion_cvt 描述符顺序修复是里程碑（累积从死到活）；涂抹根因未定，**正在执行"零 jitter 二分"（JitterMode=5 待测）**。

| 验收项 | 状态 | 修复轮次 |
|---|---|---|
| 闪烁 | ✅ 已解决 | 5ms 每输出 gate（第 12 轮） |
| 双缓冲交替 | ✅ 已解决 | 每实例门控 + 输出关联（第 7 轮） |
| 视图冻结 | ✅ 已解决 | cover_skip 300ms 过期保护（第 8 轮） |
| 位移不重合 | ✅ 已解决 | jdelay=0 + cb0 直读（第 17 轮） |
| 无 AA（累积死） | ✅ 已解决 | **motion_cvt 描述符堆顺序修复（第 28 轮）** |
| 全画面涂抹/破碎拖影 | ❌ **未解决（当前主线）** | 进行中 |
| 崩溃（相机钩子） | ✅ 已规避 | 相机钩子移除（第 32 轮） |

---

## 2. 架构

```
YuanShen.exe (D3D11, il2cpp, 内嵌 FFX_FSR2)
   │  Il2CppCallSiteHook：Render @0x06B59670 12B 绝对跳转钩子（捕获实例参数）
   │  draw 拦截：accumulate draw 的绑定采集（color/depth/motion/输出纹理）
   ▼
Dx11FsrBridge.dll（注入进程内）
   ├─ sdk234 路径（当前主线，Fsr2Sdk234=1）：
   │    draw→实例匹配 → gate → 输入 staging→D3D12 upload
   │    → motion_cvt 解码（R10G10B10A2 平方编码 → R16G16F）
   │    → ffxCreateContext/ffxDispatch（ffxApi → amd_fidelityfx_upscaler_dx12.dll，FSR2 2.3.4）
   │    → 输出 readback → UpdateSubresource 回游戏纹理
   └─ OptiScaler 翻译层（Fsr2TranslationLayer，Fsr2TranslationMode=2）：
        桥的 GetProcAddress shim 把游戏 FSR2 调用导向桥 stub（无操作）
        → 实际回落到游戏原生 FSR2（用户实证"原生有正常 AA"）
```

关键文件：
- `D:\FSR\Dx11FsrBridge\Dx11FsrBridge.cpp` — 主桥（sdk234 块约 8885-9750 行）
- `D:\FSR\Dx11FsrBridge\Fsr2Sdk234Backend.cpp` — ffx-api 后端（context/dispatch/解码/PQ）
- `D:\FSR\Dx11FsrBridge\Fsr2Sdk234AccumTest.cpp` — 离线累积测试 harness
- `D:\FSR\Dx11FsrBridge\Il2CppCallSiteHook.{h,cpp}` — Render 钩子（含相机捕获代码但已停用）
- `D:\FSR\Dx11FsrBridge\Fsr2TranslationLayer.cpp` — OptiScaler 路径（工作参照）

---

## 3. 当前配置（ini：`D:\miHoYo Games\Starward\原神解帧FSR插件包\payload\Bridge\Dx11FsrBridge.ini`）

```
Fsr2Sdk234=1
Fsr2Sdk234Version=2.3.4
Fsr2Sdk234JitterMode=5        ← 待测（零 jitter 二分；此前 3/4 均无好转）
Fsr2Sdk234JitterDelay=0
Fsr2MotionVectorScaleMode=1   (+1920；0/1 均无差异)
Fsr2Sdk234Hdr=0               （用户澄清：游戏 FSR 输入是线性格式、非 HDR）
Fsr2Sdk234AutoExposure=0
Fsr2Sdk234NonLinear=0
Fsr2Sdk234PqChain=0
Fsr2Sdk234FovScale=1.25       （0.98 rad；1.0/1.25 无差异）
Fsr2Sdk234ForceReset=0        ⚠️ 曾遗留=1 污染第 21-26 轮所有测试
Fsr2Sdk234ProbeCamera=0       （相机钩子已从代码移除）
Fsr2Il2CppHook=1
```

构建部署（当前 DLL SHA256 `0E2D80F59381BEC5B5E70EB94F052CAAF4ED2F105FD978E1E585546C4A5A0139`，含 JITTER_CANCELLATION flag）：
```powershell
. 'D:\FSR\tools\Initialize-FsrBuildEnvironment.ps1'; Initialize-FsrBuildEnvironment -WorkspaceRoot 'D:\FSR' | Out-Null
cmake --build 'D:\FSR\build-package-bridge' --config Release
Copy-Item 'D:\FSR\build-package-bridge\Release\Dx11FsrBridge.dll' 'D:\miHoYo Games\Starward\原神解帧FSR插件包\payload\Bridge\Dx11FsrBridge.dll' -Force
```
日志：`D:\miHoYo Games\Starward\原神解帧FSR插件包\payload\Bridge\Dx11FsrBridge.log`（白名单词须含 `fsr2_sdk234`）

---

## 4. 待执行测试（零 jitter 二分 — 下一会话第一件事）

**JitterMode=5（零 jitter）已设置，未测。** 在累积存活状态下：
- **涂抹消失**（只剩锯齿）→ 涂抹源 = jitter 路径（采样相位/取值）→ 深挖 cb0[28] 值语义与相位序列；
- **涂抹仍在**（全画面）→ 涂抹与 jitter 无关 → 锁定 motion 重投影/历史资源/运行时：
  - 候选 A：motion 量级 2× 过大（均值 0.0187 UV ≈ 36px）——加 motion 缩放配置 A/B；
  - 候选 B：**RDNA4 历史资源描述符问题**（运行时 FSR2 的历史纹理读错 → 累积混入垃圾）→
    **换 OPEN SDK FSR2 host 直连**（`D:\DLSSG2FSR4\dependencies\FidelityFX-SDK` 源码在手，
    用 ffxFsr2GetInterfaceDX12/DX11 + ffxFsr2ContextCreate，绕开闭源 runtime，可自验历史）；
  - 候选 C：换一个 OptiScaler runtime 版本（amd_fidelityfx_upscaler_dx12.dll 的其它构建）。

---

## 5. 三方参数审计成果（团队交付物，直接可用）

| 文档 | 内容 |
|---|---|
| `D:\Dump\work\fsr2-game-param-enum-t1.md` | 游戏侧 FSR2 参数全枚举（30 方法表、cb0 全布局、输入编码、11 项未确定） |
| `D:\FSR\fsr2_sdk_params_enumeration.md` | SDK 参数面全枚举（21 字段语义/默认/行号、flags 位错位、provider 忽略项） |
| `D:\FSR\bridge-sdk234-param-audit.md` | 桥接层逐参数处理（值+来源：game-captured/cb0/config/hardcoded/not-passed） |
| `D:\FSR\reconcile-threeway-gap.md` | 三方对照：已调整✅/未调整⚠️/从未获取❌ + 无 AA 嫌疑排序 |

**关键审计结论（摘要）**：
- ✅ 已调整：flags、renderSize/upscaleSize、color/depth/motion/output、jitterOffset(cb0)、motionVectorScale、reset、frameTimeDelta
- ⚠️ 未调整（硬编码占位）：**cameraNear=0.25 / cameraFar=6000 / FOV=π/4——游戏真实值 RE 全部未确定**（深度裁剪依赖，头号历史嫌疑之一）；viewSpaceToMetersFactor=1.0
- ❌ 从未获取：reactive / transparencyAndComposition（ini 宣称=1 但 sdk234 未接线）、exposure（AUTO_EXPOSURE 覆盖，当前=0 中性）、cb0[28].zw 曝光增益 0.6667/门控 22.0、fVelocityFactor（FSR2 host 无此字段，configure 恒失败）
- ⚠️ flags 位错位（t2）：ffx-api `NON_LINEAR_COLORSPACE(1<<8)` 实际映射 FSR2 host 的 DEBUG_CHECKING——**NonLinear=1 从未真正生效**
- 版本澄清：运行时 provider 实为 2.3.4（可用）；SDK checkout 为 2.3.2（仅源码层）；桥头 2.2.1（仅布局）——非功能缺口

---

## 6. 关键知识点（踩坑记录）

### 6.1 游戏侧事实（RE 实证）
- FFX_FSR2::Render @0x06B59670（钩子点）；Prepare 已内联（勿 hook）；ConfigureJitteredProjectionMatrix @0x06B558D0（**hook 会崩溃**——RVA 或 stub 不匹配，已放弃）
- 实例字段：jitter@0x24（**游戏内冻结**）、m_RenderSize@0x58、m_DisplaySize@0x68、frameIndex@0x50
- cb0 布局：cb0[26]=render、cb0[27]=display、**cb0[28]=(上一帧 jit_norm.xy, 曝光增益 0.6667, 门控 22.0)**、cb0[30].x=开关
- 输入：color=R10G10B10A2 **线性**（用户澄清：非 HDR/PQ；HDR/LDR 在 FSR 后独立重放）；depth=R32G8X24 逆深度（0=far，47% 为 0.0，非零 [2e-5,0.080]）；motion=R10G10B10A2 平方编码（d=raw−0.498039，mv=−sign(d)·4d²，均值 0.0187 UV≈36px）
- 相机 near/far/FOV：**全部 RE 未确定**（0.25/6000/π/4 是未验证占位）

### 6.2 SDK/运行时事实
- ffxDispatchDescUpscale 21 字段：exposure 未传→1.0；reactive/TC 未传→0；preExposure 0→1.0；viewSpaceToMeters≤0→1.0；frameTimeDelta clamp[0,1]
- provider 忽略：upscaleSize、dispatch flags、pNext；ffxConfigure 对 FSR2 恒 ERROR_PARAMETER（FVELOCITYFACTOR 仅 FSR3 有）
- 累积权重公式：`fBaseAccumulation = maxWeight × bIsExistingSample × (1−reactive) × (1−depthClip)`；重投影 `history = uv + motionVector`；深度裁剪阈值依赖相机参数
- 离线 A/B 结论（**注意离线输入是静态 dump、无相机抖动——"变化 jitter 打爆累积"结论不适用于游戏内**）

### 6.3 本项目已修 bug（勿回退）
1. **motion_cvt 描述符堆顺序**（第 28 轮，根因级）：root signature ranges[0]=SRV(t0)、ranges[1]=UAV(u0)，堆必须 SRV@0/UAV@1——此前反了 → u0 写到 motion 源（污染）、t0 读未写入的 cvt（垃圾）→ 累积死。**修复后累积活了（拖影出现 = 里程碑）**
2. ForceReset 遗留=1 污染第 21-26 轮（所有"无 AA"结果无效）——现=0
3. 相机钩子崩溃（第 31-32 轮）：stub+0x36 崩（三实例实证）——安装调用已从代码移除
4. 双配置解析块：`fsr2_*` 键在两处解析——新键只加一处会不生效（相机钩子教训）
5. context 转储（Render 的 context 参数前 512B）全零——相机参数不在该结构，勿再走此路

---

## 7. 已排除项（别再花时间）

| 变量 | 测试 | 结论 |
|---|---|---|
| motionVectorScale 符号 | mode 0/1（±1920） | 无差异（涂抹方向都不变） |
| jitter 模式 | 3（−norm×render+0.5）/ 4（−norm×render） | 无差异 |
| JITTER_CANCELLATION flag | 0x4 加/不加 | 无好转 |
| FovScale | 1.0 / 1.25 | 无差异 |
| Hdr | 1 / 0 | 死累积期无差异；现 0（线性输入正确） |
| AutoExposure | 1 / 0 | 无差异；现 0（中性 1.0） |
| PqChain | 1（白屏）/ 0 | 输入线性，无需 PQ 解码 |
| 相机钩子 / context 转储 | 崩溃 / 全零 | 均不可行 |

---

## 8. 下一步优先级（建议）

1. **【立即】零 jitter 二分**（JitterMode=5，ini 已设）——涂抹消失与否决定主线方向；
2. 【若涂抹仍在】motion 量级 A/B（加 `Fsr2Sdk234MotionScale` 配置，试 0.5/1.0/2.0）；
3. 【若仍无解】**换 OPEN SDK FSR2 host 直连**（绕开闭源 runtime——RDNA4 历史描述符嫌疑）；或换 runtime DLL 版本；
4. 【并行】相机参数获取：PRE cb0 探测（`Fsr2Sdk234Probe` 扩展）或游戏投影矩阵反推 near/far/FOV；
5. 【收尾】reactive/transparency 遮罩接入、NON_LINEAR 位修正（去 1<<8 或 host 直连设真位）、2304×1296 档离线补测。

---

## 9. 环境

- GPU：RX 9070 XT（0x7550，游戏适配器）+ AMD iGPU（0x13C0）+ Virtual Display + GameViewer
- 游戏：原神 7.0 CN（D3D11/il2cpp），会话 render 1920×1080→display 3840×2160（档位 0.5）
- 离线 harness 运行：`Set-Location 'D:\miHoYo Games\Genshin Impact Game'` 后运行
  `D:\FSR\build-package-bridge\Fsr2Sdk234AccumTest.exe`（适配器 argv：默认 0x7550，0x13C0=iGPU；
  runtime DLL 路径硬编码为 payload Bridge 目录）
- 注意：用户只在要求时跑游戏；所有构建/分析由代理完成；**用户要求过"分析后执行关机"类直接指令须执行**

---

## 10. 第三十六轮：开放 SDK 黑屏回滚（2026-08-26）

- 第三十五轮部署的静态 FidelityFX FSR2 host（`build-open-sdk-ninja-release2`）在实际游戏中全程黑屏；日志显示 `ffxFsr2ContextDispatch` 返回成功，但输出及中间资源采样为零，不能视为可渲染版本。
- 已立即回滚到上一版可渲染的闭源 dynamic runtime bridge：
  `D:\FSR\build-package-bridge\Release\Dx11FsrBridge.dll`
- 当前 payload 目标已核验 SHA256：`0E2D80F59381BEC5B5E70EB94F052CAAF4ED2F105FD978E1E585546C4A5A0139`（690,176 字节）。
- 静态 host 暂停部署；后续必须先加输出有效性 fail-open（无效输出不得覆盖游戏目标），再隔离验证资源格式/状态/host backend，确认非零输出后才能重新上线。

---

## 11. 第三十七轮：拖影量级二分（2026-08-26）

- 用户确认继续使用当前可渲染的动态 FSR2 SDK 路线；不再以替换 SDK/host 作为拖影主线。
- 新增 INI 项 `Fsr2Sdk234MotionScale`（默认 1.0，范围 0.0–4.0），只缩放已验证方向的 `motionVectorScale`；日志新增 `mvfactor`，便于确认实际值。
- 首个部署值为 `0.5`，同时保留 `Fsr2Sdk234JitterMode=5` 的零 jitter 二分；关闭 OutputMark、D3D12 debug layer 与 frame dump，避免诊断内容影响画面和性能。
- 本次 DLL 用上一版可渲染 dynamic ffx-api backend 对象重新链接，绝不含 `open-sdk-fsr2` host；payload SHA256：`B6007721EFF3A6496ED7B8F898141482F6E96CF7200952BA26BE7E878B12F0B0`（565,248 字节）。
- 判定：若 0.5 明显减少全局拖影，下一步在 0.25/0.75/1.0 收敛；若无差异，则直接转向真实 near/far/FOV 获取与深度裁剪，不再反复尝试 motion 符号或普通 jitter 模式。

---

## 12. 第三十八轮：疑似多层合成（2026-08-26）

- 用户观察拖影/模糊更像多层图像叠加。代码核验：SDK 写回 `output_target` 后，Release 版此前未安装 `CopyResource` hook；draw 级防覆盖无法看见后续 copy 链。
- Release 现已仅为 SDK 刚写过的输出安装 `CopyResource` hook：未命中时零资源查询、原样透传；命中时限量记录 `fsr2_sdk234_copy_chain role=src|dst`。`src` 表示交给后续显示链，`dst` 表示后续链正在覆盖 SDK 输出；不得据此日志前直接跳过 copy。
- payload 同时设 `Fsr2FamilySkip=1`：只在 SDK accumulate 已成功接管的短窗口内跳过原生 FSR2 家族的四个 render-size pre-pass，优先排除同帧双链预处理；不启用危险的 `Fsr2Il2CppSkipRender`。
- 当前 payload SHA256：`96841910D2BFE77C733C93E865F1EAADBB566942F610543AD667BD3EC74D5ACE`（576,512 字节），仍为 dynamic ffx-api backend，不含开放 host。

---

## 13. 第三十九轮：同 generation 双 dispatch 去重（2026-08-26）

- 用户反馈 `Fsr2FamilySkip=1` 无效；运行日志证实该开关命中 3,072 次仍无改善，且无 `fsr2_sdk234_copy_chain`，排除原生 pre-pass 与 CopyResource 覆盖。
- 日志显示同一实例同一游戏 frame 会连续进入两次 SDK（例如 frame=1），而此前 context 仅按 instance 隔离；第二次 dispatch 会把同一套 color/depth/motion 再推进一次 temporal history，符合“多层叠加/拖影”现象。
- 新增 `Fsr2Sdk234ReuseSameGeneration=1`：同一 Render generation 的第二个输出复用首个 SDK 输出（兼容纹理时 `CopyResource`），不再二次推进 FSR2 history；仍拦截原生 draw。开关设为 0 可回退旧行为。
- `Fsr2FamilySkip` 已恢复为 0，避免混入无效变量。当前 payload SHA256：`C71D48FE8099D6FDC5EBD001A252DFFDDC5A9ED07B06C92DED8828AF76B9925E`（579,584 字节），仍为 dynamic ffx-api backend。
- 观察日志关键词：`fsr2_sdk234_reuse_generation`。若拖影明显下降，说明根因是同 generation 双推进；若无变化，下一优先级为 SDK history 的双缓冲 context 隔离（按 output A/B 分 context）。

---

## 14. 第四十轮：撤销同 generation 复用（2026-08-26）

- 用户实测确认：同 generation 复用首个 SDK 输出会直接产生残像，而且残像本身是模糊的；该策略判定错误，已撤销。
- payload 已回到未包含复用逻辑的 dynamic SDK DLL：`96841910D2BFE77C733C93E865F1EAADBB566942F610543AD667BD3EC74D5ACE`（576,512 字节）。
- 配置已恢复 `Fsr2Sdk234ReuseSameGeneration=0`、`Fsr2FamilySkip=0`。当前临时开启 `Fsr2TraceColorProducers=1`，下一次运行仅用于确认 SDK 的 t0 是否已是原生时域/模糊输入；确认后应关闭。
- 重新定位：问题不再假设为输出双缓冲重复推进；优先验证输入 color 的生产者与时序，以及 SDK 是否在接收已经带历史贡献的颜色层。

---

## 15. 第四十一轮：恢复正常画质基线（2026-08-26）

- 用户提供截图确认：静态画面本身已严重模糊，转视角时残像沿运动方向出现。
- 发现上一轮测试同时启用了两个诊断变量：`JitterMode=5`（零 jitter）与 `MotionScale=0.5`；这会改变重建锐度与运动补偿，不能用于判断最终画质。
- 当前配置已回到正常基线：`Fsr2Sdk234JitterMode=4`、`Fsr2Sdk234MotionScale=1.0`、`Fsr2FamilySkip=0`、`Fsr2Sdk234ReuseSameGeneration=0`、`Fsr2TraceColorProducers=0`。DLL 未更换，仍为可渲染 dynamic SDK 版本。
- 后续若基线仍“静态模糊 + 同向残像”，将只修改 motion 解码/相机深度参数中的一个变量，不再混用零 jitter、输出复用或 pre-pass skip。

---

## 16. 第四十二轮：正常 jitter 下 motion 量级 A/B（2026-08-26）

- 用户实测：恢复 `JitterMode=4`、`MotionScale=1.0` 后静态清晰度明显提高，但动态仍模糊。
- 当前仅将 `Fsr2Sdk234MotionScale` 改为 `0.5`，其余参数保持正常基线；用于验证动态模糊是否由 motion 解码量级过大造成。

---

## 17. 第四十三轮：运动方向 A/B（2026-08-26）

- 用户实测：`MotionScale=0.5` 未解决拖影；完全静止时出现细微抖动。
- 量级过大暂时排除。当前恢复 `MotionScale=1.0`，仅将 `Fsr2MotionVectorScaleMode` 从 1 翻为 0，jitter 仍为 4；用于验证拖影方向是否由运动符号反向造成。

---

## 18. 第四十四轮：每帧 reset 历史隔离（2026-08-27）

- 用户实测 motion 方向翻转后现象完全一致，运动符号不是主因；已恢复 `Fsr2MotionVectorScaleMode=1`。
- 当前设 `Fsr2Sdk234ForceReset=1`，并保持 `MotionScale=1.0`、`JitterMode=4`。此诊断让每次 SDK dispatch 清空 FSR2 history：
  - 若静态/动态模糊显著消失，根因锁定为 FSR2 历史重投影（motion/depth/camera 参数）；
  - 若仍然模糊，根因在输入 color 已被时域/预处理污染或输入链选择错误。
- payload DLL 未更换，SHA256 仍为 `96841910D2BFE77C733C93E865F1EAADBB566942F610543AD667BD3EC74D5ACE`。

---

## 19. 第四十五轮：历史确认有效，深度方向 A/B（2026-08-27）

- 用户实测 `ForceReset=1` 后拖影消失，但抗锯齿也消失；因此 FSR2 history 是有效 AA 来源，问题集中在 history 重投影参数，而非 SDK 输出叠加。
- 当前关闭强制 reset（`Fsr2Sdk234ForceReset=0`），仅将 `Fsr2Sdk234DepthInverted` 从 1 改为 0，测试深度逆向解释是否导致运动帧历史错误保留。
- 其它变量保持：`MotionScale=1.0`、`MotionVectorScaleMode=1`、`JitterMode=4`。若拖影明显减弱且 AA 恢复，根因是深度方向；若无变化，下一步处理 cameraNear/cameraFar/FOV 占位值。

---

## 20. 第四十六轮：恢复逆深度，cameraFar A/B（2026-08-27）

- 用户实测 `DepthInverted=0` 未恢复 AA 且画面更糊，已恢复 `Fsr2Sdk234DepthInverted=1`。
- 当前新增相机参数配置并仅修改 far：`Fsr2Sdk234CameraNear=0.25`、`Fsr2Sdk234CameraFar=1000.0`（原代码占位 far=6000），FOV 仍为现有 `FovScale=1.25`。
- 目的：FSR2 深度裁剪使用 cameraNear/Far；far 占位过大可能导致运动帧历史错误保留。若拖影/动态模糊改善，再继续收敛 near/FOV；若无变化，转向 color 输入生产者与 motion 与 jitter 的帧对齐。

---

## 21. 第四十七轮：jitter 帧对齐 A/B（2026-08-27）

- 用户实测 `cameraFar=1000` 仍动态模糊，静止微抖未消除，远裁剪面不是主因。
- 当前仅将 `Fsr2Sdk234JitterDelay` 从 0 改为 1，保持 `JitterMode=4`、`MotionScale=1.0`、逆深度与 cameraFar=1000。
- 目的：验证颜色/运动纹理与 cb0 jitter 是否存在一帧错位；若微抖或动态模糊改善，继续固定正确的采样相位；若无变化，下一步禁用 jitter cancellation 或直接检查 motion 与 color 的帧同步。

---

## 22. 第四十八轮：零 motion 诊断（2026-08-27）

- 用户实测 jitter 延迟无明显区别；简单帧错位不是主因。
- 当前将 `Fsr2Sdk234MotionScale=0.0`，保留 history、`JitterMode=4`、`JitterDelay=1`、逆深度与 cameraFar=1000。
- 目的：若动态画面反而变清晰，说明 motion 解码值把历史拉向错误方向；若更糊，则 motion 信息有效，应转向深度裁剪或输入 color 来源。

---

## 23. 第四十九轮：回滚周三 22:35 后改动（2026-08-27）

- 用户指出静止微抖是在本轮对话周三 22:35 左右改动后引入；后续 `JitterDelay=1` 与 `MotionScale=0` 诊断均无改善。
- 已恢复此前最后稳定的动态 runtime DLL：`D:\FSR\build-package-bridge\Release\Dx11FsrBridge.dll`，SHA256=`0E2D80F59381BEC5B5E70EB94F052CAAF4ED2F105FD978E1E585546C4A5A0139`。
- 配置恢复：`Fsr2Sdk234JitterDelay=0`、`Fsr2Sdk234MotionScale=1.0`、`Fsr2Sdk234ForceReset=0`、`Fsr2MotionVectorScaleMode=1`、`Fsr2Sdk234DepthInverted=1`；关闭同 generation 复用、FamilySkip、TraceColorProducers。
- 该回滚用于先确认微抖是否消失；确认后再从干净基线单变量处理动态拖影。

---

## 24. 第五十轮：精确恢复周三 22:33 基线（2026-08-27）

- 用户澄清：无抖动版本为周三 22:33，而不是此前回滚目标。
- 已恢复到当时对应的 pre-same-generation 动态 DLL：`D:\FSR\build-motion-scale-dynamic-test\Dx11FsrBridge-copytrace-dynamic.dll`，payload SHA256=`96841910D2BFE77C733C93E865F1EAADBB566942F610543AD667BD3EC74D5ACE`。
- 当前配置已核验为：`ForceReset=0`、`MotionScale=1.0`、`JitterMode=4`、`JitterDelay=0`、`DepthInverted=1`、`MotionVectorScaleMode=1`、`FamilySkip=0`、`ReuseSameGeneration=0`、`TraceColorProducers=0`。

---

## 25. 第五十一轮：静止抖动的实现级定位（2026-08-27）

- 现象仍为静止画面细微抖动；周三 22:33 的精确 DLL/INI 快照未找到，现有 `22:10` 文件不能证明就是该版本。
- 代码核对发现：INI `Fsr2MotionVectorsJittered=0` 明确表示 motion 不含投影 jitter，但后端此前在 `create_context` 中无条件设置 `FFX_FSR2_ENABLE_MOTION_VECTORS_JITTER_CANCELLATION`。这会对未含 jitter 的 motion 重复补偿，符合静止抖动现象。
- 已在源码加入按 `Fsr2MotionVectorsJittered` 控制该 flag 的修复，并在桥配置初始化时传入；未部署静态开放 SDK 构建（该路线此前实际黑屏）。当前 payload 仍保持可渲染 dynamic 版本，待 dynamic 构建链恢复后再部署。

---

## 26. 第五十二轮：动态 runtime 切换 FSR4（2026-08-27）

- 保留当前可渲染的动态 `amd_fidelityfx_upscaler_dx12.dll` 与 Bridge DLL，不切换曾导致黑屏的静态开放 host。
- 已备份原配置为 `payload/Bridge/Dx11FsrBridge.before-fsr4.ini`。
- 已将 `payload/Bridge/Dx11FsrBridge.ini` 的 `Fsr2Sdk234Version` 从 `2.3.4` 改为 `4.1.1`；其余拖影/抖动参数保持不变。
- 运行时版本查询此前已确认 provider 暴露 `4.1.1`；待用户重启游戏验证渲染、静态抖动与动态拖影。若黑屏或初始化失败，恢复上述备份即可。

---

## 27. 第五十三轮：FSR4 下 jitter 中心化 A/B（2026-08-27）

- 用户确认切到 FSR4 后画质有所改善，但静止抖动与动态拖影仍存在，说明主因在桥接输入而非超分模型。
- FSR4 运行日志的稳定 dispatch 显示 jitter 约为 `-0.96875,-0.45679` 像素；游戏 cb0 中的值是 `[0,1]` 范围归一化采样，相对中心应减去 `0.5px`。
- 已备份配置为 `payload/Bridge/Dx11FsrBridge.before-jitter-center.ini`，并将 `Fsr2Sdk234JitterMode` 从 `4` 改为 `3`（保持原符号、增加 `+0.5px` 中心化）。motion/history/output 参数未改。
- 待用户重启游戏验证；若抖动明显下降，再继续评估动态拖影。若无改善，恢复备份，不继续猜测 jitter 模式。

---

## 28. 第五十四轮：FSR4 结果与拖影主线收敛（2026-08-27）

- 用户实测：FSR4 的重建能力减少了一部分模糊，但静止抖动与动态拖影仍在；因此根因不在超分模型。
- 用户观察到镜头向左时拖影也向左，符合历史重投影落到错误时空位置；但此前 `MotionVectorScale` 正负翻转、0.5 和 0.0 缩放均无改善，不能再把问题归结为简单 motion 符号/量级。
- FSR4 日志同时证明 runtime 确实为 `4.1.1`，稳定期每帧 dispatch 一次、`dt≈15–16ms`；没有模型版本回退或重复 dispatch 的证据。
- 对 `JitterMode=3` 的中心化 A/B 未解决抖动，已恢复 FSR4 基线 `JitterMode=4`。按用户要求暂不继续处理抖动。
- 下一主线：对齐调用点抓取的 `call_params` 与当前 accumulate draw 的 color/depth/motion；当前代码只按输出纹理/尺寸/最新 generation 关联，未按 frame ID 严格配对，足以使同一模型在不同版本下仍产生同向拖影。

## 29. 第五十五轮：收紧 temporal generation gate 并恢复动态 backend（2026-08-27）

- 源码在静态 open-SDK host 路线后已无法生成可渲染的 dynamic DLL；该 host 曾在实际游戏全黑，不能部署。
- 已将 `Fsr2Sdk234Backend` 恢复为 `ffx-api` runtime 路径：运行时从配置的 `amd_fidelityfx_upscaler_dx12.dll` 解析 `ffxCreateContext/DestroyContext/Dispatch/Query/Configure`，查询 provider 版本后严格选择 `Fsr2Sdk234Version`（当前仍为 `4.1.1`）。保留原来的 D3D11→D3D12 staging、motion 解码、readback 落地和每实例 context。
- 主桥 gate 只允许 `call_gen != last_gen` 时调用 SDK，删除按输出时间窗口对同一 generation 再派发的路径；并且只在 `fsr2_sdk234::dispatch` 成功后才推进 `last_gen`。这避免同一组旧 color/depth/motion 再次污染 FSR temporal history；无法证明新帧时回退既有原生路径。
- 已以 Ninja Release 构建 `D:\FSR\build-motion-scale-dynamic-test\Dx11FsrBridge.dll`，SHA256=`C4221EAA7EBAB88861C69490E0F8F116D04B0674C8A6ADAC2514F5E2E4982CCA`，仅依赖系统 D3D12/CRT，确认不静态依赖 FFX host 库。
- 已部署到 `payload\Bridge\Dx11FsrBridge.dll`；部署前 DLL 备份为 `payload\Bridge\Dx11FsrBridge.before-stale-generation-gate.dll`，SHA256=`96841910D2BFE77C733C93E865F1EAADBB566942F610543AD667BD3EC74D5ACE`。INI 未改。
- 用户验证只看两个信号：是否还出现黑屏/原生闪回；左右转镜头时，拖影是否仍与视角同向。若渲染正常但拖影无变化，下一步不再调 motion/jitter，而是实现严格 `(instance, frame_index, generation, output)` token 配对，替换现有“同尺寸+最新 generation” bootstrap。

## 30. 第五十六轮：fail-closed 拦截原生 FSR2（2026-08-27）

- 第五十五轮实际日志没有 `failed`/`fallback`，但出现多条 `fsr2_sdk234_skip reason=gate_stale`；界面切换时这些 draw 会落回原生 FSR2，造成 FSR4 蓝色边框闪烁或消失。
- 用户明确要求：无论 bridge 本次是否成功更新输出，都必须拦截原生 FSR2。
- 已把 sdk234 命中的 accumulate draw 改为 fail-closed：temporal gate 仅决定本次是否调用 SDK，不再决定是否继续执行 OptiScaler/原生 FSR2。SDK 调用失败、纹理未获取、实例未匹配或 gate stale 时都直接截断原生 FSR2。
- 已构建并部署动态 runtime DLL，SHA256=`B858186D79F15EDAB2B77D22DC26DA21A3C04AD03E7EAC1D85556BC37FB3B3EA`。上一版（第五十五轮）已备份为 `payload\Bridge\Dx11FsrBridge.before-fail-closed-intercept.dll`，SHA256=`C4221EAA7EBAB88861C69490E0F8F116D04B0674C8A6ADAC2514F5E2E4982CCA`。INI 未改。
- 验证重点：进入/离开各类 UI 与渲染界面后，蓝色 FSR4 边框不应再闪回原生或消失。该改动不试图解决拖影；下一步仍是严格帧 token 配对。

## 31. 第五十七轮：撤销 fail-closed（2026-08-27）

- 用户实测 fail-closed 在测试末切换界面后使主场景全程停留在旧帧并闪烁，证实 bridge 在这些切换窗口不能保证有可呈现的新输出；强制阻断原生会冻结目标纹理。
- 已立即回滚 payload 到第五十五轮动态 DLL，SHA256=`C4221EAA7EBAB88861C69490E0F8F116D04B0674C8A6ADAC2514F5E2E4982CCA`。
- 失败的 fail-closed DLL 已保留为 `payload\Bridge\Dx11FsrBridge.fail-closed-intercept.dll`，SHA256=`B858186D79F15EDAB2B77D22DC26DA21A3C04AD03E7EAC1D85556BC37FB3B3EA`；源码同步撤销无条件 return。
- 结论：原生 fallback 不能粗暴移除。正确路线是确保切换时把当前 draw 精确配对到可用的 bridge output，或在无有效 output 时明确放行一次原生，而不是让旧 bridge output冻结。

## 32. 第五十八轮：Render token 配对后重新启用 fail-closed（2026-08-27）

- 用户指出第五十六轮暴露的是根因，不应以 fallback 遮住；根因是调用点只保存每实例 latest snapshot，界面切换后旧实例会凭历史 output 指针认领 draw。日志中 `0x27577060:g1012` 在新实例已推进至 `g1030` 后仍被连续 `gate_stale` 认领。
- Hook 侧现为每实例保留 4 个 `{CapturedParams, generation, capture_tick}` 的 pending Render token。draw 侧先从 output 关联候选中寻找仍未消费的 token；没有则只从所有未消费、同渲染尺寸、捕获不超过 250ms 的 token 选最新者。旧 output 关联在没有 token 时失效，不能再抢走 draw。
- SDK 成功后会一次性淘汰该实例当前 token 及更旧 token，防止后来倒序消费旧 Render；失败则不消费，可诊断/重试。
- 重新启用 fail-closed：命中目标 FSR2 accumulate draw 时不再执行原生 FSR2。此时原生不应再遮住 token 配对错误；若仍冻结，日志应直接显示无 pending token 或消费异常。
- 已构建部署 DLL，SHA256=`81EA26CD95C556F549E27E021B8E6140434F91C4DD5D498139DB49979DF834CE`；上一版备份为 `payload\Bridge\Dx11FsrBridge.before-token-pairing.dll`，SHA256=`C4221EAA7EBAB88861C69490E0F8F116D04B0674C8A6ADAC2514F5E2E4982CCA`。INI 未改。

## 33. 第五十九轮：动态资源特征识别与真实分辨率（2026-08-27）

- 用户澄清：游戏渲染尺寸不是窗口输出尺寸；关系为 `render = output × 当前渲染精度`，而且精度档位/输出分辨率会变化。因此 SDK 输入尺寸不能继续使用 Render hook 捕获的 `call_params` 作为事实来源。
- `TargetUpscalerDrawInfo` 现从当前 draw 的资源特征识别 color/depth/motion/output：要求 viewport 同尺寸的 color-like output、16×16 权重、至少两张 output 尺寸 history、depth-like SRV，以及同 render 尺寸的 color/motion。t0/t2/t3/RTV1 只保留为同分候选的 tie-breaker，不再是硬编码输入。
- 普通状态镜像、on-demand 和 Release fast 路径统一调用此识别函数；sdk234 现场采集也按识别的 SRV/RTV slot 获取纹理。FSR 输入的 `render_w/h` 从 color 资源尺寸取得，`display_w/h` 从 output RTV 尺寸取得；jitter 像素换算和 motion-vector scale 同步使用这些真实 render 尺寸。若 hook 参数与资源实际尺寸不同，日志写 `fsr2_sdk234_resource_dimensions`，只诊断、不覆盖资源事实。
- 修复 token 版进入渲染场景前闪退：Render hook stub 以前在 Win64 callee shadow space (`[rsp+0x18]`) 保存 rcx，`on_render_enter` 可覆盖它，随后用垃圾 this 指针跳回原函数。现在使用 0x38 栈帧，并在私有槽 `[rsp+0x20]/[rsp+0x28]` 保存 rcx/rdx。
- 已成功 Ninja Release 构建并部署 dynamic ffx-api runtime DLL：`payload\Bridge\Dx11FsrBridge.dll`，SHA256=`7E9CA433E49AF1BBEC31970EDD88AD81044218B8D1E4FDE69511AD32ABB909FA`。部署前可渲染第五十五轮 DLL 已额外备份为 `payload\Bridge\Dx11FsrBridge.before-resource-feature-token.dll`，SHA256=`C4221EAA7EBAB88861C69490E0F8F116D04B0674C8A6ADAC2514F5E2E4982CCA`；此前所有 DLL 备份保持不动。
- 目标 accumulate draw 已识别时继续 fail-closed 拦截原生 FSR2，符合用户要求；此次首要验证是确认 ABI 修复后不再进场崩溃，并验证切界面时 token 关联不会再把旧实例的 generation 认领为当前 draw。日志重点：`fsr2_sdk234_dispatch`（含 `slots=color/depth/motion/output`）、`fsr2_sdk234_resource_dimensions` 和 `fsr2_sdk234_skip`。

## 34. 第六十轮：无 token 的交替输出缓冲修复（2026-08-27）

- 用户实测第五十九轮已基本稳定接管，但仍会短暂闪现一张旧帧后恢复。
- 最新日志没有 `failed`、原生 translation dispatch 或资源尺寸不一致；但在稳定 SDK dispatch 期间持续出现 `fsr2_sdk234_skip reason=no_match`，其 `out` 和刚写过的 `sdk234_out` 在两个输出缓冲间交替。fail-closed 当时只截断该 draw，未刷新它的目标缓冲，因此旧内容会被短暂呈现；后一个含 token 的 SDK dispatch 再覆盖它，正好对应“一闪即逝”。
- 新逻辑仅对严格条件下的此类 paired draw 修复：该 draw 必须唯一关联到已知实例、没有 pending Render token、拥有 canonical SDK output，且距成功 SDK dispatch 不超过 50ms。它只将 canonical output `CopyResource` 到当前 alternate output，绝不再调用 ffxDispatch/推进 temporal history，也不会对未知或陈旧输出做猜测复制。日志为 `fsr2_sdk234_output_repair`。
- 已成功 Release 构建并部署 DLL SHA256=`BCF83D9DA4186685E1A2C4DAFBA7252E76133ECEA7198159BBFE7A64FFA824E5`。第五十九轮 DLL 保留为 `payload\Bridge\Dx11FsrBridge.before-output-repair.dll`，SHA256=`7E9CA433E49AF1BBEC31970EDD88AD81044218B8D1E4FDE69511AD32ABB909FA`。

## 35. 第六十一轮：使用当前 Render jitter 修复运动期历史拒绝（2026-08-27）

- 用户确认旧帧闪烁处理后静态画面已基本正常，但镜头/物体运动时仍明显锯齿，说明空间重建与当前帧 AA 正常，时域 history 在运动期被拒绝。
- 本轮日志精确证明 jitter 时序错误：Render token 的 `frame=3` jitter 为 `(0.000391,0.000103)`；当前 accumulate draw 的 cb0[28].xy 却是前一帧 `(0.000130,0.000617)`，下一 draw 才出现 frame 3 这组值。此前 SDK 误将 cb0[28] 当作当前 jitter，因此 color、motion 和 FSR history 的采样相位固定错一帧；静止可收敛，但运动时会持续历史拒绝/动态锯齿。
- SDK 现优先使用已严格 token 配对的 Render jitter；仅在其非有限或不在 `[0,1)` 时才 staging 读取 cb0 作为兼容回退。正常日志 `fsr2_sdk234_dispatch` 的 `jit_src=render` 可确认生效；保留既有 mode 4 符号与当前 render 纹理尺寸换算，不混入 motion 符号或倍率 A/B。
- 已成功 Release 构建并部署 DLL SHA256=`91B335AFA2E2FB58330CE22BB478CD5FFC5AE41D8C1BF30968DB4C73C2A27CF8`。第六十轮 DLL 已保留为 `payload\Bridge\Dx11FsrBridge.before-current-jitter.dll`，SHA256=`BCF83D9DA4186685E1A2C4DAFBA7252E76133ECEA7198159BBFE7A64FFA824E5`。

## 36. 第六十二轮：补回 SDK 路径遗漏的 reactive mask（2026-08-27）

- 用户实测第六十一轮没有改善动态锯齿；`jit_src=render` 已确认生效，因此不再将 jitter 作为主因。
- 代码核验发现动态 `ffx-api` 后端只传 color/depth/motion/output，遗漏了原生 prepare 链实际传给 FSR 的 reactive mask。原生实现从 R10G10B10A2 motion 的 `B` 通道写入 R8 reactive（`Fsr2TranslationLayer.cpp` 的 `PreparedReactive = encodedMotion.z`）；这条输入此前仅在旧 translation 路径存在。
- 动态后端现由同一 motion decode compute pass 同时写入 R16G16 motion 与 R8 reactive，并以 `ffxDispatchDescUpscale::reactive` 传给 FSR4。为避免破坏既有 PQ/marker pass 的单-UAV 根签名，motion decode 使用独立的 `t0 + u0..u1` 根签名；其它路径不变。`Fsr2UseReactiveMask=1` 时启用，SDK dispatch 日志新增 `reactive=1` 作为确认。
- 已成功 Release 构建并部署 `payload\Bridge\Dx11FsrBridge.dll`，SHA256=`08A2051B47739A6D6A536727878702D939F9D6317B8C8BF955DD24F1D8BBBD99`。部署前 DLL 保留为 `payload\Bridge\Dx11FsrBridge.before-reactive-mask.dll`，SHA256=`91B335AFA2E2FB58330CE22BB478CD5FFC5AE41D8C1BF30968DB4C73C2A27CF8`。

## 37. 第六十三轮：reactive 资源绑定黑屏回滚（2026-08-27）

- 第六十二轮实测：进入游戏渲染场景全黑。该版本不能继续用于动态锯齿验证。
- 已立即将 payload 回滚为第六十一轮已可渲染 DLL，SHA256=`91B335AFA2E2FB58330CE22BB478CD5FFC5AE41D8C1BF30968DB4C73C2A27CF8`；reactive 试验源码也已撤销并重新成功构建，避免后续误部署。
- 结论：不能直接在当前 ffx-api / FSR4 context 中追加 reactive 资源后继续复用现有 descriptor/状态链；后续若恢复该输入，必须先用独立离线 host 验证 API 资源状态和 runtime 接受的可选 resource 格式，再进入游戏。

## 38. 第六十四轮：CPU 提取 reactive mask（2026-08-27）

- 先完成独立 FSR4 `4.1.1` API 探针：只读 `R8_UNORM` reactive 纹理、`COMPUTE_READ` 状态和 `ffxDispatchDescUpscale::reactive` 均被 runtime 正常接受，连续 dispatch 返回成功且输出非零。因此第六十二轮黑屏不属于 runtime 不支持 reactive。
- 新实现避开已证实有风险的 GPU UAV、附加 descriptor 和 root-signature 修改。游戏 motion 纹理本来就经 D3D11 staging 映射到 CPU：此处在同一映射中按原 translation 的语义提取每个 `R10G10B10A2` 像素 `B[20..29]`，量化为独立 render-size `R8_UNORM` upload buffer；D3D12 仅 CopyTextureRegion 到自有只读纹理，再由 `ffxDispatch` 以 `COMPUTE_READ` 传入 reactive。
- 开关仍服从已有 `Fsr2UseReactiveMask=1`；只有 motion decode 确认为 `R10G10B10A2_TYPELESS` 时传入，其他格式保持旧行为。既有 motion decode PSO、descriptor heap 和根签名未改；dispatch 日志继续带 `reactive=1` 便于确认。
- 已成功 Ninja Release 构建并部署 dynamic ffx-api DLL，payload SHA256=`FF64CC9F867DA917C447AB170619600433B7308B3D82B1313E9437F62D8FEA42`。部署前可渲染第六十一轮 DLL 已备份为 `payload\Bridge\Dx11FsrBridge.before-cpu-reactive-mask.dll`，SHA256=`91B335AFA2E2FB58330CE22BB478CD5FFC5AE41D8C1BF30968DB4C73C2A27CF8`。
- 待用户验证：进入渲染场景不能黑屏；静态画质不应回退；移动镜头时动态锯齿是否下降。若无变化，保持该稳定路径，下一步转向严格 frame/history 配对的运行期证据，不再重复 jitter、motion scale 或模型版本 A/B。

## 39. 第六十五轮：补回 transparency/composition mask（2026-08-27）

- 第六十四轮实测 reactive 已实际进入 FSR4 dispatch（日志有 `reactive=1`），但动态抗锯齿没有改善；因此它不是唯一遗漏的 temporal 输入。
- 最新运行日志确认：FSR4 `4.1.1`、当前 Render jitter、`1920×1080` motion scale、motion decode 和连续 `reset=0` dispatch 都实际生效；镜头移动段的原始 motion 也有非零幅值。不能再把本问题归为未接管、每帧 reset 或普通 motion/jitter 参数。
- 原生 prepare 链除 reactive 外还会把 T4 的 `InputFlags.Load() > 0` 作为 `transparencyAndComposition` 传给 FSR；动态路径此前完全漏掉该输入。现已用与 reactive 相同的安全 CPU 上传链补齐：按资源特征识别 render-size `R8_TYPELESS/R8_UNORM` mask（当前游戏为 T4），D3D11 staging 读回 → R8 upload → 自有只读 D3D12 texture → `ffxDispatchDescUpscale::transparencyAndComposition`。不修改 motion decode PSO、根签名或 GPU UAV。
- 已成功 Release 构建并部署，payload SHA256=`9B9A24CBAB097FC1E00A122D4F827D8E07F1920B05C6FC53384600D6A1AD73DB`。上一版 CPU-reactive DLL 已备份为 `payload\Bridge\Dx11FsrBridge.before-transparency-mask.dll`，SHA256=`FF64CC9F867DA917C447AB170619600433B7308B3D82B1313E9437F62D8FEA42`。
- 验证时日志 `fsr2_sdk234_dispatch` 应出现 `reactive=1 transparency=1`；视觉只关注动态边缘抗锯齿是否恢复，同时确认不黑屏、不退回静态模糊。若仍无变化，下一步必须针对 FSR4 runtime 的版本描述 ABI / history 配对做可观测验证，不能继续追加 mask 或改参数。

## 40. 第六十六轮：撤回 transparency/composition mask（2026-08-27）

- 用户实测确认此路径引入了新回归：拖动视角时出现与视角同向的拖影。它没有改善动态抗锯齿，不能作为当前运行版本保留。
- 游戏进程已退出后，payload 已从 `Dx11FsrBridge.before-transparency-mask.dll` 恢复为 CPU-reactive 版本，SHA256=`FF64CC9F867DA917C447AB170619600433B7308B3D82B1313E9437F62D8FEA42`。有问题的 DLL 另存为 `Dx11FsrBridge.transparency-mask-ghosting.dll`，SHA256=`9B9A24CBAB097FC1E00A122D4F827D8E07F1920B05C6FC53384600D6A1AD73DB`。
- 部署 INI 与源码包默认值均已设置 `Fsr2UseTransparencyMask=0`，后续构建不会重新启用该实验路径。CPU reactive 保持启用。
- 动态 AA 后续不得再通过新增 mask 或常规 motion/jitter 盲调推进；应先验证 FSR4 的版本描述链 ABI 或为 temporal history/token 关联补充直接可观测证据。

## 41. 第六十七轮：direct-only 结果链验收与失败恢复（2026-08-27）

- 已验收 `D:\FSR\AI智能体任务计划-执行结果.md`：其对 Render/draw 资源边界、direct FFX12 输出落地、以及 4.1.1 ABI 不能凭本地旧头文件猜测的结论成立；“不存在 direct 会话日志”不成立，最新 payload 日志已实际记录 FSR4 `4.1.1` 连续 dispatch。
- 保持用户要求的 direct-only 截断：不恢复 TranslationLayer、OptiScaler、NGX 或原生 FSR accumulate fallback。报告建议的“失败时回退原生”不采用；此前 fail-closed 旧帧冻结的根因应以精确 token/输出状态修复，而不是恢复中间链。
- 运行日志发现一个必须继续验证的事实：同一实例的 `frame=1` 连续进入两次 direct dispatch，且首个输入的 depth/motion 尚未稳定。仅按 Render generation 不能证明“每个游戏 frame 仅推进一次 history”；本轮只记录此事实，**未**贸然恢复旧的 output-copy/reuse 策略（该策略已实测产生模糊残像）。
- 已构建并部署动态 runtime DLL，SHA256=`0A67F2C8E0775538B5B732E195E66D6CFAB25A2A38ED3E3C2F506832C1635698`；之前可渲染 CPU-reactive DLL 已保存为 `payload\Bridge\Dx11FsrBridge.before-result-recovery.dll`，SHA256=`FF64CC9F867DA917C447AB170619600433B7308B3D82B1313E9437F62D8FEA42`。
- 新版本在 `Fsr2Sdk234Probe=1` 时为每次成功输出记录 `fsr2_sdk234_result rc=DISPATCH_OK gen/inst/frame/reset/ffx_rc/in/out`；失败也记录实际 FFX 返回码或“未到达 FFX”的哨兵。失败或资源无效时消费该 token、清除 canonical output/输出关联、下一枚有效 token 强制 reset，避免 TTL 内重复同一输入和旧输出 repair。原生 draw 仍截断。
- 下一次测试只需执行主场景 → UI → 主场景，并在日志中检查：`DISPATCH_OK` 的 generation 与 frame 关系、`DISPATCH_FAILED`/`RESOURCES_INVALID` 后是否紧接下一次 `reset=1`、以及是否存在同一 `inst+frame` 的两条不同 generation 成功结果。后者将决定下一步是做同 frame 输入成熟度配对，而不是继续调 motion/jitter。

## 42. 第六十八轮：禁用无 token 输出 repair（2026-08-27）

- 新会话确认旧帧闪烁与视角移动无关；日志在 `GATE_STALE/NO_TOKEN` 后持续出现两个输出缓冲间 `output_repair`，`age_ms=15..16`，说明旧帧是桥主动复制上一帧 canonical output，而非运动参数问题。
- 已禁用 `sdk234_output_duplicate` 的“无 token + 50ms 时间窗”猜测。目标 draw 仍保持 direct-only 截断，但不再执行 `CopyResource` 旧输出；待增加可证明的 paired-draw 标识后再恢复 repair。
- 已构建并部署 DLL，SHA256=`4965B7B085A2098FA10FD39BEE2DF742DA0327148C1F8B378B7569DD46FFEFEC`；上一版结果恢复 DLL 保留为 `payload\\Bridge\\Dx11FsrBridge.before-disable-output-repair.dll`，SHA256=`0A67F2C8E0775538B5B732E195E66D6CFAB25A2A38ED3E3C2F506832C1635698`。

## 43. 第六十九轮：严格 paired output repair + PQ 原值直送（2026-08-27）

- 用户实测第六十八轮完全关闭 repair 后旧帧闪烁频率升高。原因是 direct-only 截断了 alternate 输出 draw，却没有把双缓冲内容同步；显示链在两个输出之间交替读取新帧和旧帧。不能恢复无条件旧帧复制，也不能继续保持硬禁用。
- 源码恢复 `sdk234_output_duplicate`，但资格收紧为：无 pending Render token、`output_associated_inst == match_inst`、当前输出必须属于该实例已记录的 `out_a/out_b`、canonical SDK 输出必须对应最近已消费 `last_gen`（`last_sdk_output_gen == last_gen`），且最近成功 dispatch 不超过 50ms。命中时只执行兼容纹理间 `CopyResource`，不调用 FFX、不推进 temporal history；日志为 `fsr2_sdk234_output_repair`。
- 用户确认 SDK 输入是 PQ，桥侧不应额外改色。部署配置已设：`Fsr2Sdk234PqChain=0`（不做 PQ decode/encode）、`Fsr2Sdk234Hdr=1`、`Fsr2Sdk234NonLinear=1`（仅声明输入属性）、`Fsr2Sdk234AutoExposure=0`；FSR 输出交回游戏后续 LDR/HDR 重放。`Fsr2Hdr10PqColor` 仅影响实验性 TranslationLayer，不影响 SDK234 direct 路径。
- 已确认 `YuanShen` 进程未运行后完成构建部署。新 DLL：`payload\\Bridge\\Dx11FsrBridge.dll`，SHA256=`1033BF79EB4F3B035D7ECC484CC90106EA5593BC5C4E4F847971E697A3E192BE`，大小 574,976 字节。部署前第六十八轮 DLL 已备份为 `payload\\Bridge\\Dx11FsrBridge.before-strict-output-repair-pq.dll`，SHA256=`4965B7B085A2098FA10FD39BEE2DF742DA0327148C1F8B378B7569DD46FFEFEC`。
- 下一次只需进入主场景并切换一次 UI 观察：①旧帧闪烁频率是否下降；②静态/动态是否仍可渲染；③日志是否出现 `fsr2_sdk234_output_repair`，且只在同实例 `out_a/out_b` 之间复制；④`fsr2_sdk234_dispatch` 是否带 `hdr=1 nonlin=1 autoexp=0`。若闪烁仍存在，不再放宽 repair 条件，而是继续补充 paired-draw 的可证明标识。

## 44. 第七十轮：每 generation 一次 repair 与曝光路径隔离（2026-08-27）

- 最新实测日志证明 SDK234 实际参数是 `hdr=1 autoexp=0 nonlin=1`，所以当前鼠标/UI HDR 过曝不是本轮 SDK 自动曝光开启所致。直接路径没有已验证的原生 exposure 资源，依 Bridge+OptiScaler 的原则保持 `AutoExposure=0`、`preExposure=1` 和 PQ 原值直送；不增加任何桥侧色彩变换。
- 日志同时显示稳定段 `output_repair` 已累计到 1024 次，严格实例/双缓冲/50ms 条件仍不足以证明每一笔都是 paired draw。这会在同一 canonical 输出 generation 中反复 copy，造成用户观察到的不规则抽动，并使该瞬间 temporal AA 退化。
- `Sdk234InstState` 增加 `last_repair_gen`：每个成功 SDK generation 最多允许一次 alternate output repair，原有同实例、`out_a/out_b`、canonical generation 和 50ms 条件仍全部保留。此改动不影响 FFX dispatch，不会改变 history 推进次数。
- 不能将 `Fsr2TranslationMode` 设为 0：当前 direct SDK234 分支位于该入口函数内部，模式 0 会在函数开头直接退出并使 SDK234 停止运行。保持 `Fsr2TranslationMode=2` 只是为了到达入口；SDK234 分支自身仍绕过 TranslationLayer/OptiScaler。
- 已确认游戏未运行、重新构建并部署：DLL SHA256=`54FC5FE66160F08DF17C30700F96D86351139E074D40E7DB8703FB3B42A003ED`。上一版已备份为 `payload\\Bridge\\Dx11FsrBridge.before-repair-once-exposure-sync.dll`，SHA256=`1033BF79EB4F3B035D7ECC484CC90106EA5593BC5C4E4F847971E697A3E192BE`。

## 45. 第七十一轮：按 generation 顺序消费 token + 限制 dt（2026-08-27）

- 用户确认当前抽动不像旧帧叠加：无明显拖影/帧重叠，但抽动瞬间局部动态抗锯齿失效。日志显示同实例 `gen/frame` 多次从 N 跳到 N+2，说明 draw 侧总取“最新 token”时把较早 Render token 一并丢弃；该行为可使局部动态帧 history 不连续。
- `latest_pending_render_token_for` 改为按 generation 递增顺序选择最早有效 token，避免在渲染线程领先 draw 线程时跳过中间 token。
- SDK234 的 `frame_time_delta_ms` 与 Bridge 路线一致限制到 `[1,100]`，避免线程调度/抽动导致 0 或异常大 dt 破坏 temporal 权重。
- `output_repair` 的每 generation 一次限制继续保留，但不再把它作为本轮抽动主因。
- 已重新构建并部署 DLL SHA256=`6933B28C2EDDFAE334C3553126C4A5130A65FFD01D93D2A963E497931B16B78D`，上一版已备份为 `payload\\Bridge\\Dx11FsrBridge.before-sequential-token-dt-clamp.dll`。

## 46. 第七十二轮：关闭 reactive mask 验证动态 history（2026-08-27）

- 用户确认当前已不存在“抽动导致 AA 暂失”，而是稳定表现为静态有 AA、动态无 AA。最新日志已确认实际运行版 `mvscale=+renderSize`、`jdelay=0`，运动符号与 jitter 延迟不再是未验证变量。
- 当前 reactive mask 由 motion 纹理 B 通道提取；该通道在局部动态对象上可能持续为高值，会按 FSR 语义主动压低 history 权重，使动态对象始终走空间重建而无时域抗锯齿。
- 本轮仅将部署配置 `Fsr2UseReactiveMask=0`，不改 motion、jitter、depth、曝光或输出链，用于单变量 A/B。若动态 AA 恢复，下一步改为重新识别 reactive 真实来源/阈值；若无变化，恢复为 1 并转向 motion/depth 有效性验证。

## 47. 第七十三轮：FOV 60° 单变量验证（2026-08-27）

- reactive mask 关闭后动态锯齿仍存在，排除 motion B 通道遮罩持续抑制 history。当前日志已确认 token 连续、`mvscale=+renderSize`、`jdelay=0`、`reset=0` 稳定运行。
- 相机 near/far/FOV 仍是唯一未实测的 depth-clip 关键输入；此前仅测过 FOV 1.0/1.25，均无明显差异，但未覆盖 OptiScaler 常用的 60°。
- 本轮仅将 `Fsr2Sdk234FovScale` 设为 `1.333333`（π/4≈45° 基准 → 60°），保持 near=0.25、far=1000、depth inverted=1、motion/jitter、曝光和 mask 其余不变。

## 48. 第七十四轮：cameraNear=0.1 单变量验证（2026-08-27）

- FOV 60° A/B 无改善，排除常规 FOV 偏差。动态 history 仍可能因 depth-clip 相机模型错误而持续拒绝；near=0.25 尚未做有效独立验证。
- 本轮仅将 `Fsr2Sdk234CameraNear` 从 0.25 改为 0.1；far=1000、FOV=60°、depth inverted=1、motion/jitter、曝光与 mask 全部保持不变。

## 49. 第七十五轮：校正 SDK jitter 的像素中心（2026-08-27）

- 用户观察到：与视角运动方向平行的线条基本不锯齿，不平行线条出现动态锯齿。轴向核查排除了 R/G 交换和单独 Y 翻转；运行日志同时显示当前 SDK jitter 经模式 4 直接取 `-norm×render`，出现 `-0.6875/-0.4815` 等偏置值。
- 原生 accumulate cb0 明确把归一化 jitter 转为单位像素 `norm×render−0.5`；FSR2/FSR4 API 的 `jitterOffset` 同样要求以像素为单位、中心在零。模式 4 丢失了 `−0.5` 中心项并反向偏置，可能导致时域样本在运动方向上失配。
- 本轮仅修改部署配置 `Fsr2Sdk234JitterMode=0`（`norm×render−0.5`），不改 motion、depth、camera、曝光、mask、输出链或 DLL。用户启动游戏后重点观察：动态非平行线条锯齿是否下降、静态是否仍稳定、是否出现抖动/拖影/黑屏。

## 50. 第七十六轮：jitter 单项中心化回归与参数组策略（2026-08-27）

- 用户实测第七十五轮 `JitterMode=0` 导致画面抖动，并使静态抗锯齿失效；已在游戏进程退出后恢复 `Fsr2Sdk234JitterMode=4`，DLL 未变。
- 结论：不能再把 jitter、MV、投影相机视为可彼此独立的旋钮。FSR dispatch 要求同一帧的 render size、投影实际应用的 jitter（来源、符号、像素中心）、MV scale/方向、MV 是否含 jitter、depth encoding 与 near/far/FOV 共同匹配；其中一项错配即会破坏 temporal history。
- 后续不再以单个“看似官方公式”覆盖游戏实测约定。先固定已知无静态抖动的组：`JitterMode=4`、`JitterDelay=0`、`MotionScale=1`、`MotionVectorScaleMode=1`、`MotionVectorsJittered=0`、`DepthInverted=1`、`ForceReset=0`、`FamilySkip=0`、`ReuseSameGeneration=0`；再以完整、可记录的参数组进行原子 A/B，任何组切换均重置 history。

## 51. 第七十七轮：同 game-frame 重复 history 推进诊断（2026-08-27）

- 参数组核查确认：`jitterOffset`、MV scale、MV jitter cancellation、实际 render/display 尺寸与相机投影必须属于同一帧契约；当前 `JitterMode=4` 是唯一已实测不破坏静态 AA 的直接 SDK 约定，保持 `JitterDelay=0` 与 `MotionVectorsJittered=0`。
- 相机投影 hook 虽有实现但历史上因 RVA/序言风险曾导致进场崩溃，当前不重新安装它，也不继续修改视觉参数。改为先验证另一条可直接破坏时域链的事实：同一个游戏 `frame_index` 是否被多个不同 Render generation 送进 FFX dispatch。
- 本轮仅加入只读日志：成功 dispatch 后记录每实例最后的 `frame_index/generation`；若新 generation 再次使用同一 frame，会写 `fsr2_sdk234_duplicate_render_frame inst/frame/gen/previous_gen/out/count`。不拦截、不 reset、不改变资源、输出或任一 SDK 参数。
- 已确认游戏未运行，Release 构建及 `Il2CppCallSiteHookTest`、`Fsr2Sdk234BackendTest` 均通过。已部署 DLL SHA256=`7FEC821553E7BB4C52A14B4F0CF8AEA847B164B4C2DAB3C5E1718CA582FD5947`；上一可渲染 DLL 保存为 `payload\\Bridge\\Dx11FsrBridge.before-frame-contract-diagnostics.dll`，SHA256=`6933B28C2EDDFAE334C3553126C4A5130A65FFD01D93D2A963E497931B16B78D`。
- 验证只需进入主场景并平移/转动镜头约 10 秒。之后读取日志：若出现重复 frame，下一轮只处理同 frame 去重/资源成熟度；若没有，继续从真实投影矩阵采集切入，不再调任何单项视觉参数。

## 52. 第七十八轮：同 frame 诊断验收与相机目标只读核验（2026-08-27）

- 第七十七轮日志有 5,173 次成功 direct dispatch，但 `fsr2_sdk234_duplicate_render_frame` 仅出现 9 次，且全是不同实例刚建立时的 `frame=1`；稳定主场景帧号连续递增，没有持续的同 frame history 重复推进。该候选不能解释持续动态锯齿，保持诊断但不改变 gate。
- 真实相机投影仍是未验证的时域契约缺口。历史 `ConfigureJitteredProjectionMatrix` hook 曾因 ABI/序言风险导致进场崩溃，本轮不安装它、不写入游戏代码，也不改变任何 FSR 参数。
- 新 DLL 仅在启动时、`Fsr2Sdk234ProbeCamera=1` 时读取并记录该函数 RVA 前 16 个机器码字节：`fsr2_sdk234_camera_target rva/va bytes=...`。同时保留既有 context 原始探针；此轮目的是验证当前游戏二进制与历史 RVA/可安全覆盖的指令边界是否一致，之后才决定是否修复 stub 并启用相机矩阵捕获。
- 已确认游戏未运行，Release 构建与 `Il2CppCallSiteHookTest` 通过。已部署 DLL SHA256=`8E9DFCF4DEDF469306A960EF83B92682B1BE6B50836ACEBF69EFD7B57E8D72DD`；上一诊断 DLL 备份为 `payload\\Bridge\\Dx11FsrBridge.before-camera-target-probe.dll`，SHA256=`7FEC821553E7BB4C52A14B4F0CF8AEA847B164B4C2A079B52F131651123E014D`。

## 53. 第七十九轮：修复相机 hook 跨指令覆盖并采集原始矩阵（2026-08-27）

- 启动探针确认当前 `ConfigureJitteredProjectionMatrix` 序言为 `415741565657534881EC40010000440F`。前 14 字节是七个 push 加一条完整的 `sub rsp,0x140`；旧实现只覆盖 12 字节，trampoline 跳回时落在该指令中间，这是历史进场崩溃的确定性 ABI 原因。
- 相机 trampoline 已独立改为严格 14 字节覆盖、严格匹配该序言、96-byte stub 容量，并把 rcx/rdx 保存槽从 callee shadow space 移到私有 `rsp+0x20/+0x28`。关闭时也按 14 字节完整还原。修复不改变 Render hook（其既有 12 字节边界不变）。
- 已启用 `Fsr2Sdk234CameraHook=1`：它仅抓取 `ConfigureJitteredProjectionMatrix(camera)` 的原始 64 float，不写入 camera、不修改 FSR 的 jitter/MV/depth/相机参数。前 8 次及每 1024 次 SDK dispatch 日志写 `fsr2_sdk234_camera_capture frame/generation/n/...`，用于离线识别投影矩阵、实际 jitter 项和 near/far/FOV；若严格序言不匹配会拒绝安装，而非 patch。
- 已确认游戏未运行，Release 构建和 `Fsr2Sdk234BackendTest` 通过。已部署 DLL SHA256=`327E120F99CCE5CAB9D51E64A95C3228AF82AA48550ECC84A5725C5E9529EB0E`；上一只读探针 DLL 已备份为 `payload\\Bridge\\Dx11FsrBridge.before-camera-capture.dll`，SHA256=`8E9DFCF4DEDF469306A960EF83B92682B1BE6B50836ACEBF69EFD7B57E8D72DD`。

## 54. 第八十轮：相机 hook 配置生效性诊断（2026-08-27）

- 用户实测第七十九轮游戏正常运行，但日志只有 `camera_target`，没有 `camera_hook_installed` 或 `camera_capture`，说明相机采集实际上未启用，不能据此分析矩阵。
- 本轮仅增加启动配置回显 `fsr2_sdk234_camera_config probe/hook/rva`，不修改渲染逻辑、不安装新 hook。用于确认部署 DLL 读取的 INI 路径和键值是否与现场文件一致。
- 已重新构建部署，上一版 DLL 备份为 `payload\\Bridge\\Dx11FsrBridge.before-camera-config-log.dll`，SHA256=`327E120F99CCE5CAB9D51E64A95C3228AF82AA48550ECC84A5725C5E9529EB0E`。

## 55. 第八十一轮：修复相机 hook INI 段位错误（2026-08-27）

- 第八十轮回显为 `probe=1 hook=0`；代码、DLL 与键名均正确。根因是 `Fsr2Sdk234CameraHook=1` 被追加在 `[RenderScaleMenu]` 段，桥仅读取 `[Dx11FsrBridge]` 段，导致值回退默认 0。
- 已把 `Fsr2Sdk234ProbeCamera=1` 与 `Fsr2Sdk234CameraHook=1` 移到部署 INI 的 `[Dx11FsrBridge]` 相机参数块，删除错误段位的重复键；DLL 不变（SHA256=`07A06E3A973D0C46ECDFC1411A8B13BA58074B06A8935918F45F72489CC078B5`）。游戏未运行时完成配置修正。
- 下一次启动应依次出现 `camera_config probe=1 hook=1`、`camera_hook_installed` 和 `camera_capture`。该轮仍只采集相机内存，不改变 FSR dispatch 参数或画面。

## 56. 第八十二轮：相机原始内存采集验收（2026-08-27）

- 本轮日志确认 `camera_config probe=1 hook=1`、`camera_hook_installed` 成功，游戏正常运行；因此 14-byte trampoline 修复有效。
- 但 `camera_capture` 的 64 个 float 几乎全为 0，仅固定出现对象头附近的 `0.400883`，不是投影矩阵。反汇编确认该函数通过多个 Camera injected getter/setter 在栈上组装 4×4 矩阵，`rdx` 指向的是 Unity Camera 包装对象而非裸矩阵。
- 结论：near/far/FOV 仍不能从该原始快照推断；不能把 `0.400883` 当作 FOV 或 near。已关闭 `Fsr2Sdk234CameraHook=0`，避免持续 hook 开销。当前画面参数和 DLL 均不变。
- 后续若继续真实参数路线，目标必须改为捕获函数内部矩阵 setter 的 `rdx=&[rsp+0x70]` 4×4 临时数据，或从该 setter 的调用目标建立独立、严格序言验证的诊断 hook；在此之前不再修改 FSR 参数组。

## 57. 第八十三轮：捕获实际 jittered projection matrix（2026-08-27）

- 已从 `D:\Dump\game.exe` 反汇编确认正确的矩阵 setter 调用在 `ConfigureJitteredProjectionMatrix+0x380`：`lea rdx,[rsp+0x70]`、`mov rcx,r14`、`call 0x013DC510`。因此 `rcx=Camera*`，`rdx=已构造的 Matrix4x4*`；此前 `Camera*` 起始 64 float 全零的问题不再相关。
- 新增独立 projection setter hook：严格匹配目标 RVA `0x013DC510` 的完整 16-byte 序言 `48 89 5C 24 08 57 48 83 EC 20 48 8B FA 48 8B D9`，保存/恢复 rcx/rdx，且仅立即 SEH-safe 复制 rdx 指向的 16 float。序言不匹配则拒绝安装；不写入矩阵、Camera 或 FSR 参数。
- 配置：`Fsr2Sdk234CameraHook=0`，`Fsr2Sdk234ProjectionHook=1`，`Fsr2Sdk234ProjectionSetterRva=0x013DC510`。日志前 8 次和每 1024 次 dispatch 写 `fsr2_sdk234_projection_capture frame/generation/camera/n/16 floats`。这些样本将用于拟合矩阵布局、真实 jitter 符号、FOV 与 near/far；在数据验收前不向 SDK 回写任何推断值。
- 已确认游戏未运行，Release 构建与 `Fsr2Sdk234BackendTest` 通过。已部署 DLL SHA256=`A00BE8777CB2B43D20FD341BEAACEA8A84846139CCD343E7C26E9B0D8943EDED`；上一 DLL 备份为 `payload\\Bridge\\Dx11FsrBridge.before-projection-setter-capture.dll`，SHA256=`07A06E3A973D0C46ECDFC1411A8B13BA58074B06A8935918F45F72489CC078B5`。

## 58. 第八十四轮：以实测投影替换 SDK 相机参数组（2026-08-27）

- 投影 setter hook 稳定安装并捕获到完整 row-major 4×4 矩阵。例如：`[1.357995,0,0,0; 0,2.414213,0,0; jitterX,jitterY,-1.000083,-1; 0,0,-0.500021,0]`。
- 直接反推：`FOV_y=2*atan(1/2.414213)=45°`；`m00/m11=0.5625=1080/1920`，与实际 16:9 一致。深度项符合 Unity/D3D 透视矩阵，主场景约为 `near=0.5, far=6000`（加载/早期实例曾见 far≈600，主稳定场景为 far≈6000）。当前 SDK 却一直使用配置占位 `60°/0.1/1000`，这是已证实的成组投影契约错配。
- 已在游戏退出后仅调整部署 INI 的同一参数组：`Fsr2Sdk234FovScale=1.0`（45°）、`Fsr2Sdk234CameraNear=0.5`、`Fsr2Sdk234CameraFar=6000.0`；保持 jitter mode 4、jdelay 0、motion scale +render、depth inverted 1、MV non-jittered、PQ/exposure/输出链全部不变。新进程会自然重建 SDK context/history。
- 实际投影参数已取证，临时 `Fsr2Sdk234ProjectionHook` 关闭，避免常驻诊断 hook；DLL 不变，SHA256=`A00BE8777CB2B43D20FD341BEAACEA8A84846139CCD343E7C26E9B0D8943EDED`。本轮验收重点为：动态非平行线条 AA 是否恢复或显著改善，同时确认静态无抖动、无拖影/黑屏。

## 59. 第八十五轮：On12 同队列 FFX12 直通与 swapchain 资源身份修复（2026-08-28）

- 目标不变：保留游戏的状态/调用识别，但跳过原生 FSR2 的高开销 accumulate 调用；在所有需要超分的接管 draw 上改由 FFX12 / FSR4 `4.1.1` 执行。此轮没有恢复 Bridge、OptiScaler 或原生 FSR2 作为渲染回退路径。
- 已修复 On12 swapchain 的确定性资源契约错误：旧 `GetBuffer(index)` 对所有 index 返回同一个 D3D11 landing texture，缓存多个 RTV 时会发生资源别名。现在按 buffer index 建立独立的 D3D12 landing resource + D3D11 wrapped resource；最近被绑定的 landing 纹理在 `Present` 时复制到当前 DX12 flip buffer。测试同时请求 `GetBuffer(0)`/`GetBuffer(1)` 并断言 COM 资源身份不同。
- `Fsr2Sdk234Backend` 在检测到本模块创建的 D3D11On12 device 后，会复用同一 D3D12 device、同一 direct queue 与 `ID3D11On12Device2`，不再建立第二套 D3D12 device/queue。每个接管 draw 的路径为：游戏资源 `UnwrapUnderlyingResource` → GPU motion square-decode → FFX12 FSR4 → GPU copy 到 unwrapped 游戏 output → 同一 queue signal → 对每个资源 `ReturnUnderlyingResource`。On12 分支不再执行 staging `Map`、CPU memcpy、upload、readback、`UpdateSubresource` 或 D3D11 `CopyResource`。
- 输出仍暂落在桥自有的 D3D12 UAV 纹理后再以 GPU `CopyResource` 写入游戏 output；这是为了不要求游戏 output 原生带 UAV bind flag，不是 CPU 回读。游戏输入 PQ 原值保持直送；`Fsr2Sdk234PqChain=0` 时不新增 PQ/exposure 转换，`AutoExposure=0` 保持不变。
- 为避免单 command allocator 迫使每帧 CPU 等待，On12 直通使用 3 个 D3D12 allocator/list 槽位，只有 CPU 追上仍在执行的同一槽位才等待 fence。resource return fence 与槽位复用 fence 是同一个 queue timeline。
- 已确认游戏退出后完成 Release 构建并部署：`payload\Bridge\Dx11FsrBridge.dll`，SHA256=`906E5D874734074A2651F646718496E0847D04AC8F7D3DFDAD69A4E8A9ED8C50`；旧 payload 已保存为 `payload\Bridge\Dx11FsrBridge.before-on12-direct-zero-cpu.dll`。`Dx11On12SwapchainTest.exe` 通过：`PASS on12 flip swapchain feature_level=0xb000`。
- 这仅验收了编译和本机 On12 swapchain 合成测试，尚未在原神主渲染场景验收。因此不能宣称实际游戏已完成 FFX12 接管。下一次运行必须在 `Dx11On12Swapchain=1`、`Fsr2Sdk234=1`、`Fsr2Sdk234Version=4.1.1` 下检查日志：必须出现 `on12 shared d3d12 device/queue selected stage=init` 和 `on12 direct ffx12 completed ... no_cpu_bridge=1`，且不得出现 `dx11on12_sdk234_bypassed`、`DISPATCH_FAILED`、`DXGI_ERROR_DEVICE_REMOVED` 或黑屏。若 `UnwrapUnderlyingResource` 或 FFX dispatch 失败，先以该日志定位具体资源/格式；不得把 On12 分支重新接回 CPU staging 或原生 FSR2。

## 60. 第八十六轮：修复 On12 首次 dispatch 后进场前冻结（2026-08-28）

- 现象：第八十五轮 On12 直通 DLL 在游戏中**进入渲染界面之前无响应**（加载/起始画面冻结，无崩溃——`sdk234_steps.log` 未生成，异常过滤器未触发）。
- 日志定位（确定性）：`fsr2_sdk234_result rc=DISPATCH_OK gen=1 frame=1`、`on12 direct ffx12 completed count=1 no_cpu_bridge=1` 均出现，随后 `fsr2_sdk234_pipeline count=1` 是最后一行；同一 dcount==1 块内紧接着的 `fsr2_sdk234_backend_samples` / `fsr2_sdk234_rtv1_samples` 缺失 → **挂死在 `append_tex_samples(bd/r1, ...)` 诊断读回**。
- 根因：On12 模式下游戏 D3D11 设备是自建 D3D11On12、共享同一 D3D12 queue（`dx11on12_device_created transport=queue_backed`），且 `ensure_pool` 的 On12 分支不创建 `g_tex_output.d11`（`debug_output_texture()` 返回空，第一项快速返回）；第二项 `append_tex_samples(r1, output_tex, context)` 对游戏输出纹理做 `CreateTexture2D(staging) → CopySubresourceRegion → Flush → Map(READ)`——在游戏 draw 内、共享队列上发起的 CPU 同步等待，On12 runtime 下不再返回 → 主线程整体卡死。
- 修复（单变量）：`append_tex_samples` 入口新增 On12 防护——`fsr2_sdk234::interop_capabilities(dx11on12,...)` 为真时直接返回（这些 5 点采样纯诊断，On12 路径无 readback 资源本就无意义）。native-D3D11 CPU-bridge 路径（dx11on12=0）行为不变。所有调用点（probe 采样、dcount 诊断、翻译 r1 采样）经此一处统一覆盖。
- 配置按第三十九/四十一轮基线把 `Fsr2FamilySkip` 从 1 恢复为 0（该开关已被实测无效，避免混入变量）；其余 INI 不动。
- 已确认游戏未运行，重新构建并部署：payload `Dx11FsrBridge.dll` SHA256=`E0D3D8A3922CC17D8299E8D55DB9B9834588BBEFEFA1E6C1D04DD08266884B6D`（841,216 字节）。部署前第八十五轮 DLL 已备份为 `payload\Bridge\Dx11FsrBridge.before-on12-readback-fix.dll`，SHA256=`906E5D874734074A2651F646718496E0847D04AC8F7D3DFDAD69A4E8A9ED8C50`。
- 下一次验证：进入主场景前不再冻结；日志应持续出现 `fsr2_sdk234_dispatch ... slots=...` 与逐帧 `fsr2_sdk234_result rc=DISPATCH_OK`（count 递增、frame 递增），并确认 `fsr2_sdk234_backend_samples` 行照常打印（此时其采样为空，属于预期）。若仍冻结或黑屏，以新的断点日志继续定位；不得回退到 CPU staging 或原生 FSR2 路径。

## 61. 第八十七轮：推翻"DX11 无法纯 GPU 直通"结论，原生 D3D11 纯 GPU 传输上线（2026-08-28）

- **前提重审（用户要求）**：第八十六轮 On12 直通在第四次 dispatch 时仍冻结（日志证据：`fsr2_sdk234_enter count=4 frame=6` + `takeover_gap` reset 后无 result，卡死在 `dispatch_on12_direct`）。用户指出必须重新核查"DX11 模式无法纯 GPU 将 D3D11 渲染资源直通 FFX12"这个前提。
- **旧结论的来源被推翻**：2026-08-24"legacy 共享纹理在 D3D12 读为 0"用的是 `D3D11_RESOURCE_MISC_SHARED`（legacy 共享面句柄），D3D12 的 `OpenSharedHandle` 不支持该句柄类型（未定义行为→读零）。正确机制是 **`D3D11_RESOURCE_MISC_SHARED_NTHANDLE` + `IDXGIResource1::CreateSharedHandle` + `ID3D12Device::OpenSharedHandle` + D3D11.4 共享 fence（`ID3D11Device5::OpenSharedFence`）**。
- **GpuInteropProbe 离线实证（本机 RX 9070 XT，LUID 0x123A3 = 游戏同适配器）**：重写探针，逐格式字节级验证——共享 fence `PASS`；D3D11 侧建 SHARED_NTHANDLE 纹理 → D3D12 打开 → D3D11 GPU 写 → D3D12 GPU 读回 `PASS`（R8G8B8A8/R10G10B10A2_TYPELESS/R16G16_FLOAT/**R32_FLOAT** 全过）；D3D12 UAV 写（与 FFX 输出同用法）→ D3D11 GPU 读回 `PASS`。**唯一不支持：`R32G8X24_TYPELESS` 建共享纹理（E_INVALIDARG）与 D3D12→D3D11 直接导入（0x80070057，生产不需要）**。
- 第三方佐证：[brycehutchings/D3DSharingTests](https://github.com/brycehutchings/D3DSharingTests)（N 卡实测）"sharing from D3D12 to D3D11 does not work in many cases where sharing from D3D11 to D3D12 does work"；[OptiScaler Known Issues](https://github.com/optiscaler/OptiScaler/wiki/Known-Issues#directx-11-with-directx-12-upscalers)（D3D11 游戏用后台 D3D12 设备 + D3D11on12，不劫持游戏设备）。
- **新传输 `dispatch_gpu_shared`**（游戏设备保持原生 D3D11，不劫持为 On12）：
  1. 深度：游戏 R32G8X24 不可共享 → 在游戏 context 上注入 D3D11 CS 提取到共享 R32_FLOAT（恰好是 FFX12 深度 SRV 格式，无需 typeless cast）；
  2. 颜色/运动：D3D11 GPU `CopyResource` → 共享纹理（R10G10B10A2_TYPELESS）；`Flush` + `ctx4->Signal(f11, v1)`；
  3. 自建 D3D12 队列 `Wait(f12, v1)` → motion 解码（共享 motion SRV）→ `ffxDispatch`（color/depth/motion 共享 SRV、共享输出 UAV，参数与 On12 路径逐项一致）→ 全部归还 COMMON → `Signal(f12, v2)`（ring 槽位另信 g_fence）；
  4. `ctx4->Wait(f11, v2)` → D3D11 GPU `CopyResource`(游戏输出 ← 共享输出)。
  全场 **零 CPU staging / Map / UpdateSubresource**；fence 单调推进、跨 API 交接契约（COMMON 归还 + 有序等待）与探针验证路径一致。
- 配置：引入 `Fsr2Sdk234GpuInterop=1`（默认开；=0 回退旧 CPU staging 路径）；**`Dx11On12Swapchain=0`**（不再劫持游戏设备为 On12——进场冻结根因移除）；`append_tex_samples` 的 On12 防护保留（原生 D3D11 下不触发）。
- 已确认游戏未运行，重新构建并部署：payload `Dx11FsrBridge.dll` SHA256=`53CE3E8FF276C6207C2E9002443AFB21A9C141110A3FCEED428E3B41EA916540`（854,528 字节）。上一版（第八十六轮读回修复）已备份为 `payload\Bridge\Dx11FsrBridge.before-gpu-interop.dll`，SHA256=`E0D3D8A3922CC17D8299E8D55DB9B9834588BBEFEFA1E6C1D04DD08266884B6D`。
- 构建环境备注：`Initialize-FsrBuildEnvironment` 在 `WindowsSDKDir` 已由 vcvars 设置时会把 `WindowsSDK_LibraryPath_x64` 拼成不存在的 `...\10\um\x64`（漏 `Lib\<version>\` 段）→ LNK1104。修复：构建前显式设 `$env:WindowsSDK_LibraryPath_x64 = "$kits\Lib\$ver\um\x64"`、`$env:UniversalCRT_LibraryPath_x64 = "$kits\Lib\$ver\ucrt\x64"`。
- 待用户验证：进入主场景前不再冻结；日志首行附近应出现 `gpu interop ready stage=init`（sdk_msgs）与桥侧 `fsr2_sdk234_transport ... gpu_interop_ready=1`、`on12_device=0`；稳定段持续 `fsr2_sdk234_dispatch ... DISPATCH_OK`（帧号递增、无黑屏/闪退）。此轮同时证明**On12 swapchain 引导不是必须的**，纯 GPU 链路在原生 D3D11 下成立。

## 62. 第八十八轮：恢复版本边框标记（FSR2 红 / FSR3 绿 / FSR4 蓝）（2026-08-28）

- 现象：用户实测第八十七轮原生 D3D11 GPU-interop 传输**正常运行**（日志确认：7168 次连续 `DISPATCH_OK`、`on12_device=0 gpu_interop_ready=1`、`backend_samples` 与 `rtv1_samples` 逐点一致、实例切换正常、无冻结）；但**画面内的版本边框标记消失**，用户无法直观判断当前跑在 FSR2/3/4 哪个模式。
- 机制定位：版本标记 pass 自 2026-08-24 起实现于旧 CPU staging 路径——`g_marker_hlsl` 在 `g_tex_output_enc`（自有 enc 纹理）上画 **边框 24px + 左上角块 110px**，颜色按版本编码（`g_marker_234/315/411_pso`：2.3.4 红 / 3.1.5 绿 / 4.1.1 蓝），开关 `g_output_mark`（INI `Fsr2Sdk234OutputMark`）。第八十七轮的 `dispatch_gpu_shared` FFX **直写共享输出纹理 `g_tex_output.d12` 且未画标记** → 标记凭空消失（非 OSD 文字窗口，是画面内色块）。
- 修复（改动最小、三路径共用）：
  1. 把 marker 描述符堆创建抽成 `ensure_marker_heap_for(ID3D12Resource *enc)`（Heap = [UAV@0, SRV@1]，shader-visible；UAV 指向调用方传入的 enc，画标记时按需覆盖），旧 CPU 路径改为调用同一 helper。
  2. `ensure_pool` 的 GPU-interop 分支：`Fsr2Sdk234OutputMark=1` 时对共享输出建 marker heap。
  3. `dispatch_gpu_shared`：ffxDispatch 返回后、归还 COMMON 前插入标记 pass——把 heap 的 UAV 覆盖为 `g_tex_output.d12`（typed `g_output_enc_format` 视图，UAV 不能用 TYPELESS 格式），按 `marker_pso_for_version()` 选色，Dispatch(display/8)。标记随共享输出经 D3D11 CopyResource 落回游戏 rtv1 → 全程可见。
- 配置：`Fsr2Sdk234OutputMark=1`（默认开，与用户既有认知一致；=0 关闭不画）。FFX 参数、传输、fence 均未改（单变量）。
- 已确认游戏未运行，重新构建并部署：payload `Dx11FsrBridge.dll` SHA256=`C91BA3DC2A7D7843345806D119E77F77342802AB9590D5045956802B9138D257`（855,552 字节）。上一版备份为 `payload\Bridge\Dx11FsrBridge.before-marker-restore.dll`，SHA256=`53CE3E8FF276C6207C2E9002443AFB21A9C141110A3FCEED428E3B41EA916540`。
- 待用户验证：画面角落出现版本色边框/角块（当前配置 FSR4 → 蓝色）；切 `Fsr2Sdk234Version=2.3.4/3.1.5` 应分别变红/绿，确认标记与日志 `version=` 一致。

## 63. 第八十九轮：修复版本标记 pass 的描述符布局崩溃（2026-08-28）

- 现象：第八十八轮（恢复 OutputMark）**进入渲染界面之前游戏闪退**；上一轮（无标记、GPU-interop）运行 7168 次 dispatch 正常 → 新引入的标记 pass 是唯一变量。
- 根因（确定性代码缺陷）：marker pass 复用 `g_mv_rs` root signature，其 descriptor 布局为 **SRV(t0)@desc0 + UAV(u0)@desc1**（第 28 轮修复过的顺序）；但 marker 描述符堆旧实现（2026-08-24 遗留、从未真实启用——OutputMark 此前部署一直为 0）建成了 **[UAV@0, SRV@1]**。root table 解析时 `u0` 绑定到 **SRV 描述符**、`desc0` 以 t0 引用 UAV → 描述符类型不匹配 → 驱动层崩溃闪退。
- 修复（两处对齐）：
  1. `ensure_marker_heap_for`：heap 布局改为 **[SRV(enc)@0, UAV(enc)@1]**，与 g_mv_rs 一致；CPU 旧路径（画在 enc 上、不覆盖 UAV）同步受益。
  2. `dispatch_gpu_shared` 标记 pass：UAV 覆盖目标改为 **heap start + 1 个描述符**（desc1），加 `g_mv_rs` 非空防御。
- 已确认游戏未运行，重新构建并部署：payload `Dx11FsrBridge.dll` SHA256=`4BF130CA7CA7182CAADF13EE918519966BFA7C4AD64E3467725D1F57D512A9D1`（856,064 字节）。上一版备份为 `payload\Bridge\Dx11FsrBridge.before-marker-layout-fix.dll`，SHA256=`C91BA3DC2A7D7843345806D119E77F77342802AB9590D5045956802B9138D257`。
- 待用户验证：进场不再闪退；FSR4 → 画面蓝色边框/角块；切版本颜色随动。若仍闪退，交回 `sdk234_steps.log`（异常过滤器在崩溃时写游戏 cwd）定位。

## 64. 第九十轮：撤销画面内版本标记，改 OSD 悬浮文本（用户决策）（2026-08-28）

- 现象：第八十九轮修正 marker 描述符布局后**仍进场闪退**（第八十七轮无标记基线 7168 次 dispatch 稳定；第八十八/八十九轮唯一新增变量均为标记 pass）→ 判定画面内标记（在共享输出上 dispatch 覆盖）写法不稳定。
- 决策（用户指令）：放弃画面内标记，改用 **OSD 悬浮文本窗口**（独立置顶半透明窗口，不干涉游戏画面）。
- 实施：
  1. 移除 `dispatch_gpu_shared` 中的标记 pass 块与 `ensure_pool` GPU-interop 分支的 marker heap 创建；`Fsr2Sdk234OutputMark=0`。标记 PSO/helper 保留（旧 CPU 路径仍引用，配置关闭时不执行）——GPU-interop 路径回到第八十七轮已验证稳定的形态（与之前只有 OSD 文本变化）。
  2. OSD 增强（`update_osd_sdk234`）：文本改为显示**真实 runtime 版本 + 传输模式**（`FSR 4.1.1 ACTIVE disp=N` / `transport=gpu-shared(NT) gpu_only` / `render/display`），替代硬编码 "FSR2 2.3.4"；刷新时机增加 `dcount==1`（首次接管立即显示，此后每 15 次 dispatch 刷新）。OSD 窗口为 WS_EX_TOPMOST|LAYERED 悬浮文本，仅 GDI 绘制，与 D3D11 输出无关，天然不稳定敏感。
- 已确认游戏未运行，重新构建并部署：payload `Dx11FsrBridge.dll` SHA256=`2D8466DE3C23D6E5A01D530D51D740D77AA4BF849BD9C4058D3A006CB71543D5`（855,040 字节）。上一版备份为 `payload\Bridge\Dx11FsrBridge.before-marker-removal.dll`，SHA256=`4BF130CA7CA7182CAADF13EE918519966BFA7C4AD64E3467725D1F57D512A9D1`。
- 待用户验证：进场不闪退（回到第八十七轮等价渲染路径）；屏幕左上角出现 OSD 悬浮窗，显示 "FSR 4.1.1 ACTIVE" 与 transport；切 `Fsr2Sdk234Version` OSD 版本随动。

## 65. 第九十一轮：OSD 在 Release 构建从未启动的根因修复 + 窗口跟随（2026-08-28）

- 现象：用户实测 OSD 悬浮文本**不可见**（无边框窗口运行只可见游戏画面）；当前渲染稳定不闪退。
- 根因（代码级）：`start_osd()`/`set_osd_text()` 被包在 `#if !defined(DX11FSRBRIDGE_RELEASE_RUNTIME)` 内，而部署的 payload 均为 Release runtime 构建（该宏开启）→ **OSD 线程从未启动**，与窗口层级/GameViewer 无关。已把两行调用移到条件编译之外（`g_config.show_osd` 内部仍 gate）。
- 可见性增强：新增 `osd_anchor_to_game_window()`——OSD 定时器（250ms）把悬浮窗定位到**同进程游戏主窗口（UnityWndClass）左上角内侧**并强制 `HWND_TOPMOST`，适应无边框/串流场景；`start_osd` 加入口日志 `osd_started`。
- 帧率/垂直同步边界（用户要求"应遵守游戏本身设置、指令下发"，代码核验后明确）：桥**从不调用 Present、不劫持 swapchain、不改 SyncInterval**——Release 部署下 swapchain/Present hook 的全部激活条件（`HookPresent`/`FinalSceneProbe`/HDR spoof/force/DLSSG workaround 等）默认关闭（`should_hook_present=false`）。桥不施加任何帧率上限；是否锁显示器刷新率完全由游戏"垂直同步/帧率上限"设置决定。桥的 out_a/out_b 双缓冲仅为 GPU 端输出纹理轮换（FSR 结果交替承接后 CopyResource 回游戏 rtv1），**不参与扫描同步**，不产生锁帧效果。
- 已确认游戏未运行，重新构建并部署：payload `Dx11FsrBridge.dll` SHA256=`F1717987C1C6974B11214AA48878FFE9E1A242E15A377940573A36FAD465812D`（942,080 字节）。上一版备份为 `payload\Bridge\Dx11FsrBridge.before-osd-release-fix.dll`，SHA256=`2D8466DE3C23D6E5A01D530D51D740D77AA4BF849BD9C4058D3A006CB71543D5`。
- 待用户验证：重启游戏后 OSD 悬浮窗应出现（日志含 `osd_started`）；内容显示 `FSR 4.1.1 ACTIVE` + `transport=gpu-shared(NT)`；帧率行为与游戏 VSync 设置一致（开 VSync 锁刷新率、关则不锁）。

## 66. 第九十二轮：日志级验收——确认 FSR4 运行且无回退原生渲染（2026-08-28）

- 用户要求改为**日志验证**为主（OSD 不可见不再追究）：只要日志证明运行在 FSR4 模式、无回退原生渲染即可。
- 验收依据（`payload\Bridge\Dx11FsrBridge.log`，02:35–02:39 运行，dispatch 计数至 45,056）：
  - **FSR4 模式**：每条 `fsr2_sdk234_path count=N version=4.1.1 chain=pq_decode->ffx_dispatch->pq_encode`；`fsr2_sdk234_dispatch ... version=4.1.1 mvscale=... jit_src=render`；`gpu interop ffx12 completed count=... no_cpu_bridge=1`（45 个打印点对应全部 45,056 次 dispatch 成功）。
  - **传输**：`fsr2_sdk234_transport ... on12_device=0 gpu_only=1 gpu_interop_ready=1`（原生 D3D11 + NT 共享句柄纯 GPU 传输）。
  - **无回退/无失败**（全文计数）：`failed`=0、`DISPATCH_FAILED`=0、`fallback=original|translation_blocked|no_fresh_producer`=0、`bypassed|DEVICE_REMOVED`=0。
  - **接管连续性**：帧号连续推进；跨实例（0x741CD80→0x2577A9A0 等）；多处渲染档位切换（1920×1080→1600×900→1152×648→2304×1296）全部成功接管；`backend_samples` 与 `rtv1_samples` 逐点一致（输出真实落回游戏画面）。
  - 说明：`takeover_gap` 触发的 `fsr2_sdk234_reset` 是界面/场景切换窗口期的 gate 行为（主动重置 history 后继续接管，fail-closed 拦截保持），不是回退原生。
- 此轮无源码/配置变更；OSD 可见性问题（Release 下窗口与串流捕获的兼容性）暂挂起，用户不再要求。

## 67. 第九十三轮：重启用 FSR2 5-PS 家族 PRE 拦截（画质主线）（2026-08-28）

- 用户反馈画质问题：静态 AA 基本完美、**动态 AA 失效**（FSR4 下动态无 AA，FSR2 下动态变糊，比游戏自带 FSR2 差），并指出历史 OptiScaler+Bridge 路线通过**补拦截 4 个遗漏的渲染路径**解决过。
- 历史依据（`D:\Dump\work\probe-verdicts-20260822.md` / `phase1-family-takeover-design.md`）：游戏 FSR2 = 5 连发合成 PS pass（PRE-1..4 render-size 预处理 + 累积 `78057a29`）；现桥经典只截断累积、PRE 照跑 = **双跑残余（E1）**。用户记忆的"4 条路径"即 PRE-1..4。
- 动作：INI `Fsr2FamilySkip=0→1`（单变量；DLL 无需重编，逻辑自第 38 轮在 `Fsr2FamilyTakeover`/draw hook 中）。第 38 轮判"无效"是在 CPU-staging+参数未对齐基线；现 GPU-interop 全对齐基线下重测。
- 用户实测：**有细微改善，但不是主因**——PRE 拦截保留（双跑消除是正确修复），继续找主因。

## 68. 第九十四轮：frameTimeDelta（dt）高精度帧号门控修复（2026-08-28）

- 主因候选（日志证据）：稳定段 `fsr2_sdk234_dispatch ... dt_ms=` 在 1.0/15.0/47.0/100.0 间跳变，从未稳定于帧时长——旧实现按"两次 dispatch"的 `GetTickCount64` 墙钟间隔计时：同帧多 dispatch（alternate 双缓冲/多视图）得 ~0ms（clamp 1），跨帧受 15.6ms 时钟分辨率量化（1/15/47 假象）。FSR 的 frameTimeDelta 语义 = 游戏帧时长（运动矢量按帧归一化的时间步），dt 乱跳 → **运动时 temporal 权重/运动归一化错 → 动态 AA 失效（静态无运动不受影响，与现象完全吻合）**。
- 修复（`Dx11FsrBridge.cpp` sdk234 块）：
  1. 改 **QPC 高精度计时**（微秒）。
  2. **游戏帧号门控**：`frame_index` 变化时 dt = 实测真帧间隔；**同帧重复 dispatch 沿用上次 dt**（同一时间步），且只在帧号变化时推进计时基准 → 交替输出 A/B 的第二次 dispatch 不再把 dt 打成 0。
  3. 首帧兜底 16.7ms；clamp [1,100] 保留；失败失效路径（`invalidate_direct_attempt`）同步清零新时间基。
- 保留家庭拦截（`Fsr2FamilySkip=1`）与所有既有参数；本轮仅 dt 一处变更。
- 已部署：payload `Dx11FsrBridge.dll` SHA256=`B14BEE3D83711AD453B09D72A1EB564779AE161048AEF5F95BBC927267B3D691`（946,688 字节），备份 `payload\Bridge\Dx11FsrBridge.before-qpc-dt.dll`（SHA256=`F1717987C1C6974B11214AA48878FFE9E1A242E15A377940573A36FAD465812D`）。
- 待验证：①日志 `dt_ms` 应稳定于帧时长（60fps→≈16.7，30fps→≈33，不再 1/47/100 乱跳）；②动态画面 AA（FSR4 动态边缘、FSR2 动态清晰度）应明显改善；③`fsr2_family_skip_draw` 持续命中（PRE 拦截生效）。若动态 AA 仍差，下一候选：输入同源性（采样纹理 vs 原生 accumulate 消费的 PRE 链输出）、reactive mask 恢复、以及 motion 与 depth 的逐帧时序核验。

## 69. 第九十五轮：motion 幅值诊断——发现解码输出恒 0 的硬故障（2026-08-28）

- 用户实测 dt 修复对画质无影响（排除）。转向 motion 通路，新增低频 readback 诊断（每 1024 dispatch）：`gpu motion stats cvt_mean_uv/cvt_max_uv/cvt_center/raw_mean|d|/raw_mean|mv|/raw_nonzero/mvscale`。
- 结果（判定性强）：
  1. `cvt_mean_uv=0.000000` 全程——**喂给 FFX 的运动恒 0**（此前所有"MotionScale 0.5/1.0 无差异、符号翻转无差异"由此完全解释：FFX 从未收到运动）。
  2. `raw_mean|d|=0.704` 恒定且超 UNORM 理论上限（max 0.502）——共享侧内容异常；`Fsr2Sdk234Probe=1` 对照证明**游戏原始 motion 纹理完全正常**（转动 mvmax=0.13~0.15、静止 0.01~0.04、值域/符号正常逐帧变化）→ 断点在桥内共享拷贝/解码链路。

## 70. 第九十六轮：金丝雀证实 D3D12 解码 pass 从不写 cvt → 改 D3D11 侧解码（2026-08-28）

- 金丝雀：新增 `Fsr2Sdk234MotionDecodeTest`（D3D12 motion 解码 shader 常数注入输出 (0.5,0.5)，无视输入）。实测**开启后 cvt_mean_uv 仍 = 0** → 解码 pass 未写 cvt（或描述符/UAV 断）——该 D3D12 decode pass（连同 On12 时代）**从未真正输出过运动**；"静态 AA 靠 jitter 相位累积、动态全废"的历史根因定位。
- 修复（D3D11 侧对称方案）：motion 解码改在**游戏原生 D3D11 设备上用 compute 执行**（与深度提取完全对称）：`g_motion_decode_cs`（t0=游戏 motion R10G10B10A2 SRV → u0=UAV）写共享 `g_tex_motion_cvt_share`（R16G16_FLOAT，SHARED_NTHANDLE，probe 已验证可共享）；D3D12 打开后作为 FFX `motionVectors`（R16G16_FLOAT）输入。D3D12 decode 段（inputs/PSO/描述符）从 `dispatch_gpu_shared` 移除；raw motion 共享拷贝删除（解码 CS 直接读游戏纹理）。
- 已部署：payload `Dx11FsrBridge.dll` SHA256=`9B8428C2FBEB6302EB259390CE93D1CB36148A607FA4B999BD5C4A03F966271C`（954,880 字节），备份 `payload\Bridge\Dx11FsrBridge.before-d11-motion-decode.dll`（SHA256=`7C61874168908CFEB611535E78F092BD3E1E653F37B532F3ED04823BA0B7395F`）。`MotionDecodeTest=0`（金丝雀关闭）。
- 待验证（用户测）：`gpu motion stats` 的 `cvt_mean_uv` 转动时应**显著非零**（这是 FFX 真实收到的运动）→ 动态 AA（FSR4 动态边缘/FSR2 动态清晰度）应恢复；静止≈0、转动随镜头变化。

## 71. 第九十七轮：motion 修复验收 + Reactive 在 GPU-interop 下重新接通（2026-08-28）

- **用户实测里程碑**：D3D11 侧 motion 解码上线后，**动态锯齿大幅缓解**——"静态完美、动态全废"的历史病根（D3D12 decode pass 从不写 cvt）确认修复。
- 优化阶段（用户选定"全部按推荐顺序"）：①版本对照（2.3.4 vs 4.1.1，零成本）→ ②reactive 重估 → ③精修。
- **②reactive GPU 化实现**（历史"reactive 无改善"结论全部基于 motion=0 坏基线，前提已变，重开）：
  - 在 D3D11 motion 解码 CS 中加 **u1 输出 reactive**（`out_reactive = c.b`，motion B 通道 bits 20..29，与历史 CPU 提取语义一致：`b10/1023 → R8`）。
  - 新增共享 `g_tex_reactive_share`（R8_UNORM，SHARED_NTHANDLE，D3D12 打开），`dispatch.reactive`（`FFX_API_SURFACE_FORMAT_R8_UNORM`）仅在 `input.use_reactive_mask` 时传入；inputs 列表按开关加入。
  - 全 GPU、与 motion/深度提取同机制；`Fsr2UseReactiveMask` INI（桥侧 `sdk_in.use_reactive_mask`）开关。
- 已部署：payload `Dx11FsrBridge.dll` SHA256=`84940EB791BE342DFF420D2B0CACF022B85A219D280570A1C4E69A158BFEC848`（957,952 字节），备份 `before-reactive-gpu.dll`。
- 待验证：①版本对照结果；②reactive A/B（`Fsr2UseReactiveMask=1` vs 0，重点观察动态物体边缘稳定/拖影/鬼影）；日志 `reactive=` 字段应为 1。

## 72. 第一百零贰/壹佰零叁轮：竖纹（静止时间累积网格）诊断闭环 + MotionScale 三态对照（2026-08-28）

- 竖纹现象收敛：静止时屏空间固定条带（亮暗交替、云"刺猬化"）、运动时消失成"掠过纹理"（=FSR4/原生都有的固有覆盖层，非 bug）；仅 Bridge FSR2/3（0.2~0.8 档）与 FSR4（0.2/0.3 档）出现；原生 FSR2 任何档位都没有。
- 排除项（A/B 验证）：jitter 中心/模式（mode0/3/5 无效）、motion 直流（cvt_mean_xy≈(2.2e-5,3.9e-5) 极小）、depth 全 0（探针 bits3x3 全 00000000；DSV 在 accumulate 时已解绑拿不到——游戏绑定深度纹理本身全 0，原生同样环境）、AUTO_EXPOSURE（无效）、motion 死区（无效）、ForceReset=1（**竖纹消失**→时间累积是产生器）。
- **MotionScale 三态对照**（决定性）：0.5 竖纹轻、1.0 竖纹最重（残差~0.5px 恰在 FSR lock 临界带 → 锁/不锁反复切换）、2.0 无竖纹（超阈值永续刷新）。**主力配置（FSR4 + 0.5 档 + MotionScale=1.0）动态锯齿最小且无竖纹 = 最优**。低档位避害选配：2.0（静止干净、动态略过冲）或 0.5（竖纹轻、动态略欠）。
- 新增 `Fsr2Sdk234MotionDeadzone`（motion<0.0005UV 归零，触发静止锁定）——实测无效，默认 0 保留实现。

## 73. 第一百零肆轮：里程碑大清理（只保留 GPU-only 路线）已部署（2026-08-28）

- **删除**：dispatch_on12_direct（On12 全链：on12 引导/queue/资源）、CPU staging 全链（stage_copy/upload buffers/g_tex_*_own/decode_lut/ensure_input_staging）、D3D12 motion 解码 pass（g_mv_* / g_tex_motion_cvt / 金丝雀 PSO）、版本标记 marker 全链（g_marker_* / marker PSO 编译）、PQ 颜色空间全链（g_pq_* / g_tex_output_enc / pq PSO 编译——GPU 路径本就直喂 PQ）、sample_motion_stats（每 1024 次 GPU readback 阻塞 + CPU 遍历全图——性能项，曾引发 TDR）、fsr2_sdk234_jitter_trace（高频诊断日志）。
- **保留**：dispatch_gpu_shared（NT 共享句柄 + D3D11.4 fence 纯 GPU 传输）、D3D11 侧 motion 解码（含 deadzone 变体）、深度提取 CS、reactive 共享输出（配置开关）、低频状态日志（transport/failed+原因/result@1024/path@gpu_interop ready/device-removed 恢复）、OSD、版本多开（2.3.4/3.1.5/4.1.1）、设备移除自动恢复。
- dispatch() 收敛为 GPU-only 单路径：gpu interop 未 ready → 记因返回（fail-open 回游戏原生 FSR）。
- 桥侧 swapchain 的 On12 分支为配置门控（Dx11On12Swapchain=0 不执行、零开销），保留未动（控制风险）。
- 已部署：`Dx11FsrBridge.dll` SHA256=`D0F43A7FC7AA486BA744FC9CB288FDD62E54687E6C5BCA0AF94A7493A8DF316D`（896,512 字节；清理前 967,168 → **-70KB**），备份 `before-clean-104.dll`。INI 基线：Version=4.1.1、MotionScale=1、JitterMode=4、Deadzone=0、Reactive=0、Probe=0、On12=0、ShowOSD=1。
- 待验证（用户跑一次）：功能无回归——`fsr2_sdk234_transport ... gpu_only=1 gpu_interop_ready=1`、`fsr2_sdk234_result rc=DISPATCH_OK`（低频）、无 `DISPATCH_FAILED`；表现应与清理前一致（FSR4 0.5 档动态最优）。日志应不再出现 `gpu motion stats` / `jitter_trace` 行。

## 74. 第一百零伍轮：与 OptiScaler 共存兼容（解决"OptiScaler FG 未激活 + 游戏黑屏"）（2026-08-28）

- **诊断结论**（基于 `payload\OptiScaler\OptiScaler.log` + `payload\Bridge\Dx11FsrBridge.log` 07:03-07:05 会话）：
  1. **FG 未激活**：OptiScaler `Libraries.OptiDllPath`=外层 `payload\OptiScaler`，而 FSR 提供者全集在子目录 `payload\OptiScaler\OptiScaler\` → `LoadProxyLibrary` 全部 "Can't find → nullptr" → `InitFfxDx12_SR/FG LoadResult: false`；且游戏原生 D3D11（桥保持原生，Dx11On12Swapchain=0）→ `Failed to get ID3D12CommandQueue from pDevice` → Dx11wDx12 FG 通道不通（`fg 887A0001` 设备挂起尝试）。
  2. **黑屏**：桥默认 FFX 路径为 `g_module_dir\..\OptiScaler\amd_fidelityfx_upscaler_dx12.dll`（外层）——OptiScaler 更新后其 SDK DLL 移入子目录 → **外层路径失配 → LoadLibrary 失败（err=18，被 OptiScaler loader hook 劫持）** → 每帧 `DISPATCH_FAILED` → 桥 direct-only gate 仍截断原生 accumulate → 游戏无输出 → **黑屏**。
- **修复（已构建，等部署）**：
  1. 默认 FFX 路径改为 **Bridge 目录自身副本** `g_module_dir\amd_fidelityfx_upscaler_dx12.dll`（文件在，28.7MB）——不再依赖 OptiScaler 目录布局。
  2. **提前加载**：桥 `initialize()`（DllList 顺序桥在先、OptiScaler 注入前）即 `fsr2_sdk234::preload()` LoadLibrary 缓存句柄 → init_locked 复用，避开 OptiScaler 的 LoadLibrary hook（err=18 根除）。
  3. **vendor 自动路由**（`Fsr2Sdk234AutoRouteVendor=1` INI）：主适配器 NVIDIA(0x10DE)/Intel(0x8086) → `set_passthrough(true)`：桥不初始化 FFX、draw hook 入口原样放行 → OptiScaler 接管（N 卡路线）；AMD(0x1002) → 桥 FFX12 直连照常（AMD 不需要 OptiScaler）。passthrough 在 draw hook 最前（hooked_draw_indexed 入口）判定。
- 已构建：SHA256 待部署后记录；备份 `before-optiscaler-compat.dll`；INI `Fsr2Sdk234AutoRouteVendor=1`。
- 待验证：①AMD 上（本次环境）加 OptiScaler 共存 → 不再黑屏、桥 FFX12 正常接管（日志 `route vendor=amd passthrough=0` + `gpu interop ready` + `DISPATCH_OK`）；②N 卡上 → `route ...passthrough=1`，OptiScaler 接管（其 FG 需先修 OptiScaler.ini `OptiDllPath`→子目录，且 D3D11 游戏 FG 通道需 D3D11On12 决策）。

## 75. 第一百零伍轮补：黑屏归因修正——FG 开启触发 Dx11wDx12 设备挂起（2026-08-28）

- **用户对照实证**：`Starward\原神解帧FSR插件包`（A，`FGInput=Upscaler`+`FGOutput=FSRFG` 显式开 FG）黑屏；`D:\原神解帧FSR插件包`（B，`FGInput=auto`+`FGOutput=auto`→nofg 未开 FG）OptiScaler 正常接管。
- **黑屏机制定案**：A 开启 FG → OptiScaler 对 D3D11 游戏激活 FG 必须先做 **Dx11wDx12 swapchain 升级**（日志 `Failed to get ID3D12CommandQueue from pDevice` → `CreateSwapChainForHwnd failed: 887A0001` = DXGI_ERROR_DEVICE_HUNG）→ 呈现链设备挂起 → **黑屏**。未开 FG（B）不触 swapchain 升级 → 正常。
- 桥侧 `LoadLibrary ffx-api err=18` 是同一会话的并存问题（默认路径指向 OptiScaler 外层已失效 + OptiScaler loader hook），与 FG 黑屏叠加；兼容修复（路径改 Bridge 目录、提前加载、vendor 路由）已部署：payload `Dx11FsrBridge.dll` SHA256=`37BED33773BC5854E6D0445785C7FDA583B658AB3F62254A299B2CE5D3FEBE58`（897,536 字节），备份 `before-optiscaler-compat.dll`，INI `Fsr2Sdk234AutoRouteVendor=1`。
- 止血方案（当前配置可用 FG 之前）：A 份 `OptiScaler.ini` 对齐 B——`FGInput=Upscaler→auto`、`FGOutput=FSRFG→auto`（→nofg），即不触发 Dx11wDx12 升级，不黑屏。真需要 FG：需 D3D11On12 路径（Dx11On12Swapchain=1）且与桥互操作需专项验证（N 卡场景由 vendor 路由自动交 OptiScaler）。

## 76. 第一百零陆轮：彻底隔离旧 OptiScaler+Bridge 方案 + ffx12 命名规范（2026-08-28）

- **架构决策（用户）**：完全隔离旧"OptiScaler+Bridge 翻译层"方案——旧代码**不再编译进 DLL**；新架构仅保留 ffx12（ffx-api on D3D12）GPU-only 链路；OptiScaler 未来以 ffx12 为基底重新接入（N 卡路线经 vendor 路由让路）。
- **编译剔除**：
  - CMake：`Fsr2TranslationLayer.cpp/.def`（旧翻译层）与 `Dx11On12Swapchain.cpp`（On12 引导）从构建移除（宏 `DX11FSRBRIDGE_ENABLE_FSR2_TRANSLATION_EXPERIMENTAL` 保留——sdk234 接管块在该宏内）。
  - 桥：删除 `dispatch_fsr2_translation` 两处调用（probe 函数整体 #if 0；try_fsr2_translation_draw 内旧翻译层执行段 #if 0，sdk234 失败路径改为**直接 return false** 放行）、全部 `dx11on12_swapchain::` 10 处调用（含 swapchain 创建 hook 的 On12 分支）、FSR2 GetProcAddress shim（`install_fsr2_get_proc_address_shim` 与查询日志）。
  - 修复了 #if 0 段 CRLF 替换失败导致的预处理器错位、`first_target`/`info` 变量 if 块括号、`reset_fsr2_translation_context` 旧上下文重置块移除等编译问题。
- **ffx12 命名规范**：`set_sdk_version` 接受 `ffx12-fsr4.1.1 / ffx12-fsr3.1.5 / ffx12-fsr2.3.4`（自动映射回 SDK 枚举名 `4.1.1/3.1.5/2.3.4` 做版本匹配）；显示名统一 `ffx12/FSR4` 等品牌；`g_version_name` 默认 `ffx12`。INI `Fsr2Sdk234Version=ffx12-fsr4.1.1`（兼容旧值直接输入）。
- **INI 整理**：移除旧方案键（Dx11On12Swapchain/EnableFsr2GetProcAddressShim/Fsr2TranslationMode/Fsr2FastStateTracking/Fsr2Mode2OnDemandState/BlockDx11On12Upscalers/DlssgDxgiWorkaround——代码默认值等于原值，行为不变）；板块重写为「OptiScaler 兼容（新架构）」+「DLSS/XeSS（预留）」占位说明。
- 已部署：payload `Dx11FsrBridge.dll` SHA256=`345D38270C03CA990A7A79DB8F1AD302A74243C02CFEF6FF1B3DAC040A6A6EB2`（**764,416 字节**，较上版 897,536 **-133KB**；二进制已确认不含 Fsr2TranslationLayer 符号），备份 `before-legacy-removal.dll`。
- 版本映射修正（接管失败后）：INI `ffx12-fsr4.1.1` 首次映射只剥 `ffx12-` 前缀，剩 `fsr4.1.1` 与 SDK 枚举名 `4.1.1` 不匹配 → `requested ffx-api version unavailable` → 全部 DISPATCH_FAILED。修复：`set_sdk_version` 映射链再剥 `fsr` 前缀（`ffx12-fsr4.1.1 → 4.1.1`）。最终部署：payload `Dx11FsrBridge.dll` SHA256=`F3D53B4E835D76A03525234FBCD8288CD10BAAF80CEB8A35D7404ECF6CDD584C`（764,416 字节），备份 `before-version-map-fix.dll`。
- **用户实测：超分接管成功**（第一百零陆轮闭环）。OSD 显示 `ffx12/FSR4`；后续 OptiScaler 的新接入将以 ffx12 基底单独设计（N 卡经 vendor 路由让路）。

## 77. 第一百零柒轮：内置 RDNA2（RX 6000）FSR4 支持——4.0.2c provider 双版本路由（2026-08-28）

- **背景**：RDNA2 上 4.1.1 provider 不可用（ffx-api 内部 GPU 校验静默回落 FSR3；且该架构有驱动编译器回归→ghosting）。4.0.2c 世代无强校验，OptiScaler 社区实测 RDNA2 可跑真 FSR4。
- **4.0.2c 来源核实**：
  - AMD 官方 GitHub（GPUOpen-LibrariesAndSDKs/FidelityFX-SDK）v2.0.0（2025-08-20）即 FSR 4.0.2 官方公开发布（`FidelityFX-SDK-v2.0.0.zip` ~125MB，GitHub releases 免登录直下）；包内 upscaler provider 版本串为 **4.0.2**（signedbin 13.1MB `4.0.2.44888` 签名 Valid / bin 12.5MB `4.0.2.45326` 未签名），**无 "c" 后缀**。
  - 用户流通的 4.0.2c（41MB `4.0.2.0`，版本串含 "4.0.2c"，签名 UnknownError）为社区版（OptiScaler Discord/泄露线），**非官方仓库内容**。
  - **实测证据**：绝区零（原生 FSR4）根目录替换该 4.0.2c 后，`Player.log` 明确 `[ffx] Upscaler Version: 4.0.2c`——证明 ffx-api 版本名即 `4.0.2c`，桥 `set_sdk_version("ffx12-fsr4.0.2c")` 直接匹配（绝区零目录内 SHA256=DC07B803...C6CA98 与 E:\Downloads 完全一致；原 4.1.1 被改名为 `.1dll` 留作备份）。
- **实施**：
  - payload\Bridge 新增 `amd_fidelityfx_upscaler_dx12_402c.dll`（= 用户 4.0.2c，41,043,920B，SHA256=DC07B80328950AA8DAF0A97F8CD7BA9976BBA9749E50E4F8E2F1F10219C6CA98）。
  - 桥 `initialize()` 新增 RDNA2 路由块（在 preload 之前）：AMD 主适配器 + DeviceId 命中 RDNA2 段（Navi21/22/23=0x73A0-0x73FF，Navi24=0x743F-0x7444）→ 覆盖 `set_sdk_dll_path(402c)` + `set_sdk_version("ffx12-fsr4.0.2c")`；日志 `fsr2_sdk234_route vendor=amd rdna2=1 device=0xXXXX version=4.0.2c`。**注意 0x744C 起已是 RDNA3（Navi 31/7900 XTX），Navi24 上限 0x7444，勿误伤**。
  - 后端 `set_sdk_version` 显示名：`4.0.2c` 归入 `ffx12/FSR4`。
  - INI 新增：`Fsr2Sdk234Rdna2Auto=1`（RDNA2 自动切 4.0.2c，默认开）、`Fsr2Sdk234Dll402c=`（可覆盖 4.0.2c DLL 路径，默认随包）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`D55920EBE72758EFCA86A53960AE609FA90C835F523DF3485E13B3072CD9A2ED`（765,952 字节，+1.5KB），备份 `before-rdna2.dll`（= F3D53B4E，106 轮版本）。
- **待验证（需 RDNA2 机器）**：日志 `fsr2_sdk234_route ... rdna2=1 ... version=4.0.2c` + `rc=DISPATCH_OK` + OSD `ffx12/FSR4`；RDNA2 精度路径（无矩阵加速，provider 自动选型）需实测观察。RDNA4（9070 XT 0x7550）不受影响，仍走 4.1.1。

## 78. 第一百零捌轮：正式版架构——大版本判定 + SDK 迁移 payload\AMD + INI 正式化（2026-08-28）

- **版本判定简化（SDK 更新免适配）**：
  - `set_sdk_version` 改为大版本解析：`ffx12-fsr4.x / ffx12-fsr3.x / ffx12-fsr2.x`（兼容旧精确值 `4.1.1/3.1.5/2.3.4`，同样归入对应大版本）。内部存 `g_sdk_version_prefix`（"4."/"3."/"2."）。
  - `init_locked` 版本匹配循环改为**前缀匹配**：`strncmp(version_names[i], prefix, 2)`——"4." 命中 4.0.2c/4.1.1/未来 4.2.x 首个。删除 `g_sdk_version_name` 字段与匹配成功后对 `g_version_name` 的覆盖（品牌显示名由 set_sdk_version 固定）。
  - RDNA2 路由改 `set_sdk_version("ffx12-fsr4.x")`（4.0.2c 由前缀命中）。
- **SDK 迁移 payload\AMD**：
  - `payload\Bridge\amd_fidelityfx_upscaler_dx12.dll`（4.1.1.2740，28.7MB）与 `amd_fidelityfx_upscaler_dx12_402c.dll`（4.0.2c，41MB）移至 `payload\AMD\`（Bridge 目录只留桥本体 + INI + 日志）。
  - 默认路径：`load_config` → `g_module_dir.parent_path()/L"AMD"/L"amd_fidelityfx_upscaler_dx12.dll"`；RDNA2 块 402c → `parent_path()/L"AMD"/L"amd_fidelityfx_upscaler_dx12_402c.dll"`。`Fsr2SdkDllPath`/`Fsr2Sdk234Dll402c` 键仍可覆盖。
  - OptiScaler 不受影响（`OptiDllPath` 指向自身子目录，自带 4.1.1.2740 同版）。
- **显卡自动路由（正式语义）**：AMD 直连 FFX12——RDNA4（RX 9000，如 9070 XT 0x7550）→ 4.1.1 自动 FP8；RDNA3（RX 7000，0x744C+）→ 4.1.1 自动 INT8；RDNA2（RX 6000，0x73A0-0x73FF / Navi24 0x743F-0x7444）→ 4.0.2c；NVIDIA/Intel → passthrough 交 OptiScaler。
- **INI 正式化**（重写为正式版，备份见 git/旧版）：板块 = 基础 / 显卡自动路由 / 画面参数 / 链路内部参数 / 渲染精度菜单。**删除全部诊断/实验键**（Probe/CameraHook/ProjectionHook/ProjectionSetterRva/ForceReset/DecodeTest/MotionDecodeTest/MotionDeadzone/DebugLayer/DumpFrames/OutputMark/DumpPixelShaders/Fsr2FamilyExpireMs/CaptureMetadataOnly/Fsr2Il2CppSkipRender 等，代码默认值=原 INI 值，行为不变）；`Fsr2Sdk234Version=ffx12-fsr4.x`。
  - 注意：`Fsr2JitterMode/Fsr2Hdr10PqColor/Fsr2UseNativeExposure/Fsr2FastMetadataCopy/Fsr2CompactLinearOutput/Fsr2LockColorProducerShader/Fsr2ResetOnColorPathChange` 有**活跃引用**（jitter 计算 7617、颜色格式 10996、曝光传递 10882、输出链 10931、reset 11158）——归入"链路内部参数"保留，勿删。
  - 代码默认值对齐：`Fsr2Sdk234ProbeCamera 1→0`、`Fsr2Sdk234JitterDelay 1→0`、`Fsr2Sdk234ReuseSameGeneration 1→0`（对齐正式推荐值）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`5703EDD878A7FBE8F3603D17786892D9C4BDF15C20066EBC41057DA2A7920E4F`（766,464 字节），备份 `before-stable-108.dll`。
- **待办建议**：① Bridge 目录 45+ 个 `before-*.dll` 历史备份建议归档到包外（正式版瘦身）；② 代码层物理删除已无引用的诊断读取（本轮仅 INI 层剥离，读取保留默认值=关）；③ RDNA2 实测（日志 `rdna2=1 version=4.x` + `rc=DISPATCH_OK`）。
- **旧 Fsr2* 键核查（用户质询"是否已被 sdk234 键取代"）**：逐引用点核实——
  - **可删（引用全在死代码）**：`Fsr2Hdr10PqColor/Fsr2UseNativeExposure/Fsr2FastMetadataCopy/Fsr2CompactLinearOutput/Fsr2ResetOnColorPathChange` 的引用全部位于 `#if 0`（10839-11531，旧翻译层执行段）与 `#if !RELEASE` 区；`Fsr2LockColorProducerShader` 编译存在但条件 `fsr2_translation_mode >= 4` 恒假（RELEASE 默认 0）。对应功能已由 `Fsr2Sdk234Hdr/AutoExposure/NonLinear/PqChain` 接管 → 已从正式版 INI 移除。
  - **必须保留**：`Fsr2JitterMode=3`——`target_jitter_pixels()`（7599）在活跃 sdk234 dispatch 输入链（10529）被调用，从游戏 cb0 读取原生 jitter 并做符号修正；与 `Fsr2Sdk234JitterMode`（sdk234 生成模式）职责不同，两者并存。

## 79. 第一百零玖轮：源码层面全面同步 ffx12 命名（2026-08-28）

- **用户要求**：从源码层面起把 FSR2SDK234 同步为 FFX12（第一百零六轮仅对外品牌/INI 命名，本轮贯彻到源码标识符）。
- **重命名映射**（282 处，区分大小写精确替换，0 残留）：
  - `fsr2_sdk234` → `ffx12`（216 处：命名空间 `ffx12::`、g_config 字段 `ffx12_*`、日志 `ffx12_route/ffx12_result` 等）
  - `Fsr2Sdk234` → `Ffx12`（66 处：INI 键 `Ffx12Version/Ffx12GpuInterop/Ffx12Rdna2Auto/Ffx12Dll402c/...`、类型引用）
  - `Fsr2SdkDllPath` → `Ffx12DllPath`
  - 文件重命名：`Fsr2Sdk234Backend.cpp/.h → Ffx12Backend.cpp/.h`（CMake 同步），测试文件 `Fsr2Sdk234*Test.cpp → Ffx12*Test.cpp`
  - 保留 `Fsr2*` 前缀（游戏原生 FSR2 拦截语义，非 ffx12 链路）；宏 `DX11FSRBRIDGE_ENABLE_FSR2_TRANSLATION_EXPERIMENTAL`、target 名 `Dx11FsrBridge` 不变
- **INI 同步**：正式版键名全部改为 `Ffx12*`（`Ffx12=1`、`Ffx12Version=ffx12-fsr4.x`、`Ffx12Rdna2Auto=1` 等）；`Fsr2MotionVectorScaleMode/Fsr2JitterMode` 等保留（游戏原生语义）。
- **备份**：源码备份 `D:\FSR\backups\round109-src\`（22 文件）；DLL 备份 `before-ffx12-rename.dll`。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`3A0CCD37EC998D48BA8A30E93AA92F17F948C9628D1F61284BBEAB759F234F80`（765,440 字节）。二进制确认无 `fsr2_sdk234` 字符串，日志键为 `ffx12_route/ffx12_result`。
- **注意**：日志行前缀已变（`fsr2_sdk234_route` → `ffx12_route`），排查时以新前缀为准；交接文档历史轮次中的旧日志引用仍指同一事件。

## 80. 第一百一十轮：A 卡 + OptiScaler 共存实测核查 + route 日志可见性修复（2026-08-28）

- **用户实测**：A 卡（9070 XT）+ OptiScaler v0.9.4 同时注入，核查兼容路径（第一百零伍轮 preload + vendor 路由）是否生效。
- **日志证据**：
  - 桥：`ffx12_result rc=DISPATCH_OK` 持续（count 至 19456），`gpu interop ready stage=init`，**无 `LoadLibrary ffx-api failed err=18`** → preload 生效，LoadLibrary 全程成功。
  - OptiScaler：`OptiScaler v0.9.4-final loaded`、`Upscalers.Dx11Upscaler: fsr31_12 / Dx12Upscaler: fsr31`、`NVSDK_NGX_D3D12_CreateFeature Creating new fsr31 upscaler` + `init successful for fsr31`（D3D12 特性创建成功）；`CreateSwapChainForHwnd Failed to get ID3D12CommandQueue, creating Dx11 swapchain`（swapchain 升级失败优雅降级，**无黑屏**）。
- **结论**：A 卡场景桥（D3D11 draw 级 ffx12 直连）与 OptiScaler（D3D12 fsr31 特性）各自独立链路并行，**无冲突** → 印证"正常超分模式下本身不会冲突"。第一百零伍轮的 err=18 是历史特定场景（OptiScaler 更新后 SDK 路径失配 + loader hook 劫持 LoadLibrary），当前架构（preload + AMD 直连 + 各自 provider 句柄）已消除。
- **发现问题并修复**：日志中无 `ffx12_route` 行——initialize() 中 vendor/RDNA2 路由的 log_line 在 `reset_log()`（truncate）之前输出被清空，且 RELEASE 构建 log_line 有过滤（basic_terms 含 "ffx12"）。修复：新增全局 `g_route_summary`，路由判定时存摘要，`reset_log()` 后 `Dx11FsrBridge active` 之后补打 `ffx12_route <摘要>`。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`0E3D1A8B8F3C0C96ABDB847247078B95D58BC9B453BB81C7B345A857703A9441`（765,952 字节），备份 `before-route-log-fix.dll`。
- **下轮验证**：重启游戏后日志应可见 `ffx12_route vendor=amd passthrough=0 (FFX12 direct)`（A 卡）/ `vendor=0x10DE passthrough=1 (OptiScaler takeover)`（N 卡）/ `vendor=amd rdna2=1 ...`（RDNA2）。

## 81. 第一百一十一轮：vendor=0 修复——D3D11 设备级路由补检（2026-08-28）

- **问题**：第一百一十轮 route 日志修复后实测，日志显示 `ffx12_route vendor=0x0000 passthrough=0`——initialize() 在 DllMain attach（loader lock 内）调 CreateDXGIFactory1/EnumAdapters1 失败 → vendor 检测失效。后果：N 卡失去 passthrough 保护（可能重回与 OptiScaler 冲突/黑屏）、RDNA2 无法触发 4.0.2c 路由。
- **修复**：路由逻辑抽为统一应用点 `apply_adapter_route(vendor, device, source)`（AutoRouteVendor/Rdna2Auto 门控保留原语义：RDNA2 应用不受 AutoRouteVendor 影响）；新增 `route_from_d3d11_device()`——用 `ID3D11Device→IDXGIDevice::GetAdapter→GetDesc` 可靠检测（不受 loader lock 影响）。initialize 早期检测 vendor=0 时不标记 applied，D3D11 设备创建 hook（hooked_create_device_and_swapchain / hooked_create_device 的 SUCCEEDED 块）补检并应用。前置声明补齐。
- **行为**：early 成功 → 一次性应用（source=early）；early 失败 → 设备创建后应用（source=device）。日志 `ffx12_route <摘要> source=early|device`。
- **部署**：payload `Dx11FsrBridge.dll` SHA256 见部署输出，备份 `before-device-route.dll`。待用户重启验证：A 卡应见 `ffx12_route vendor=amd passthrough=0 (FFX12 direct) source=early|device`。

## 82. 第一百一十二轮：Ffx12ForcePassthrough 强制让路开关（2026-08-28）

- **需求**：测试 A 卡也走 passthrough（桥完全不参与、OptiScaler 全权接管）功能是否正常——为 A 卡共存对照 / N 卡兜底准备。
- **实现**：`apply_adapter_route()` 开头新增 `Ffx12ForcePassthrough=1` 检查——无条件 `set_passthrough(true)`（跳过 vendor/RDNA2 判定，vendor=0 也立即生效），日志 `ffx12_route forced passthrough=1 (OptiScaler takeover) source=...`。
- **INI**：正式版新增 `Ffx12ForcePassthrough=0`（默认关；测试完改回 0）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`E9B478822484196C9718B6779F1E0A77635855AECD46AB0CDDEF22AC7A83C863`（766,976 字节），备份 `before-force-passthrough.dll`。
- **测试预期（A 卡 + Ffx12ForcePassthrough=1）**：桥日志仅 `ffx12_route forced passthrough=1`，无 `ffx12_result/DISPATCH_OK`（桥完全不参与）；OptiScaler 接管画面；功能正常即通过。
- **第一百一十二轮功能移除（2026-08-28 当日实测）**：A 卡 + `Ffx12ForcePassthrough=1` 实测——**passthrough 导致 OptiScaler 识别不到 FFX 输入**（原神 D3D11 的 FFX 输入链与桥的行为耦合：桥完全不参与后 OptiScaler 的 FFX 输入 hook 失效）。功能有反作用，**已移除**（代码块 + INI 键删除）。结论：passthrough 仅限 N/Intel vendor 自动路由（N 卡上游戏走 DLSS/XeSS 输入，不依赖 FFX 链）；A 卡必须由桥直连。部署 SHA256=`486C10DD46F54EB84E8CBE02F333C66E7D8DF3FF5ABBF1CC0E115617A2A5B35C`，备份 `before-passthrough-removal.dll`。

## 83. 第一百一十三轮：passthrough 机制整体移除（2026-08-28）

- **背景（用户澄清机制）**：OptiScaler 的工作机制是 hook 游戏原生的 FSR/DLSS/XeSS 接口，再转换为任意 DLSS/FSR/XeSS 输出；而**在原神里，FSR 接口由桥（Bridge）实现**（桥的 D3D11 draw 拦截链就是 FSR 接口本体）。passthrough 让桥不实现接口 → OptiScaler 的 hook 目标消失 → 识别不到 FFX/FSR 输入。因此 passthrough 机制本身（非仅 A 卡强制开关）有反作用，**整体移除**。
- **移除范围**：
  - 后端：`g_passthrough` 变量、`set_passthrough()/passthrough()` API（含头文件声明）、`init_locked` 与 `dispatch` 入口的 passthrough 短路。
  - 桥：`hooked_draw_indexed` 入口的 passthrough 原样放行块；`apply_adapter_route` 的 N/Intel set_passthrough(true) 分支与 `Ffx12AutoRouteVendor` 门控（键从 INI 移除）。
  - 日志：`passthrough=0/1` 措辞改为 `direct (FFX12)`；N/Intel 现在同样 `vendor=0x10DE direct (FFX12)`。
- **共存模型（新）**：所有显卡统一桥直连 FSR4（RDNA4→FP8、RDNA3→INT8、RDNA2→4.0.2c）；N/Intel 需要 DLSS/XeSS 时共存 OptiScaler——桥始终实现 FSR 接口（OptiScaler 的 hook 输入依赖它），OptiScaler 在接口之上转换输出（第一百一十轮实测 A 卡并行无冲突）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`639D6A69E17D0BD6B96A8E59BB5DFACB98BAB937CBC3E1D3ECA29BFA6339FD85`（765,440 字节），备份 `before-passthrough-mech-removal.dll`。
- **后续**：N 卡 OptiScaler DLSS/XeSS 共存实测（桥 FSR4 直连 + OptiScaler 转 DLSS/XeSS 的完整链路验证）。

## 84. 第一百一十四轮：版本切换"未生效"根因定位——OptiScaler ffx 接口层改写（2026-08-28）

- **现象**：`Ffx12Version=ffx12-fsr3.x`（及 2.x）切换后日志显示变了，但画面/帧数与 FSR4 一致。
- **排查**：写 VersionProbe（GET_VERSIONS + 各版本 override 创建上下文 + fpMessage）计划实证 provider 行为——未及完成，用户实测定位根因。
- **根因（用户实测）**：**同时接入 OptiScaler 时**，OptiScaler 通过 ffx 接口接入（对 FSR 具体版本不敏感），统一输出为默认 FSR4（OptiScaler `UpscalerIndex=0` + RDNA4 自动 Fsr4Update）。**移除 OptiScaler 后验证实际 FSR 成功降级**——即桥的版本 override（2.x/3.x/4.x 大版本前缀匹配 + ffxOverrideVersion）本身工作正常，无代码 bug。
- **共存语义确认**：OptiScaler 在 ffx 接口层做版本改写（FSR 输入 → FSR4 输出）；桥直连时其 Ffx12Version 决定真实执行版本。两者同时启用时 OptiScaler 的改写优先于桥的版本选择。
- **动作**：临时诊断文件 VersionProbe.cpp 归档 `D:\FSR\backups\round113\`（源码目录保持干净）；无需代码改动。

## 85. 第一百一十五轮：N 卡版本降级链（4.x→3.x→2.x）+ 降级显示名同步（2026-08-28）

- **N 卡用户日志（E:\Downloads\Dx11FsrBridge.log）分析**：
  - `ffx12_route vendor=0x10DE direct (FFX12) source=device`（N 卡直连，passthrough 移除后正常）；
  - **每帧 `ffx_rc=-2147483647` + `requested ffx-api version unavailable stage=runtime`** → 硬失败，无降级；
  - 根因：4.1.1 provider 的 GET_VERSIONS 列表**按 GPU 能力过滤**——N 卡无 FSR4 官方支持（仅 RDNA4/RDNA3 dGPU），列表不含 "4.x"；桥请求 `4.` 前缀匹配失败 → init 失败 → 全部 DISPATCH_FAILED。
- **修复**：版本匹配改**降级链**——请求前缀无匹配时 4→3→2 依次尝试（例：N 卡请求 4.x → 自动回落 3.1.5），sdk_note 记录 `ffx-api version 4.x unavailable on this GPU, falling back to 3.x`；降级结果同步 `g_version_name`（OSD 显示实际版本 ffx12/FSR3，不误导）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`494F0B64E70056510B3527518DF634F1BC3558336D00703C23ED37E68ED2F7A9`（766,464 字节），备份 `before-version-fallback.dll`。
- **预期（N 卡复测）**：请求 4.x → 日志 `falling back to 3.x` + OSD `ffx12/FSR3` + `rc=DISPATCH_OK`（FSR3.1.5 实际执行）；"FSR4 INT8" 最优路径取决于 provider 在 N 卡的 FSR4 支持面（当前 4.1.1 校验下不开放，需另行评估 4.0.2c INT8 强制路径）。

## 86. 第一百一十七轮：GPU 名称模糊匹配路由 + 模拟测试（41/41 通过）+ 中文注释编码修复（2026-08-28）

- **路由改为名称模糊匹配**（对齐芙芙启动器 Install-FufuPlugin.lua 的 gpu_matches_any 规则，弃用设备 ID 段——太严格）：
  - 402c 组（4.0.2c provider）：AMD RX 6xxx（RDNA2）、未列型号 AMD 核显（"Radeon Graphics"/"Radeon(TM) Graphics" 变体）、NVIDIA GTX 16 / RTX 20-50 系、Intel Arc（独显+核显）
  - 默认组（4.1.1）：AMD RX 9xxx（RDNA4）、RX 7xxx（RDNA3）、RDNA3 核显型号（740M/760M/780M/8040S/8050S/8060S/840M/860M/880M/890M，lua int8_rules）、PRO W9/W7、其他（降级链兜底）
  - 实现：`classify_gpu_arch(vendor, desc)` + `contains_ci`（大小写不敏感子串）；日志 `ffx12_route vendor=0x10DE arch=nvidia16_50 gpu="NVIDIA GeForce RTX 5090" -> 4.0.2c provider`（含 GPU 名，便于校正）。
- **后端装载一致性修复**（6950 XT 首次失败根因）：`g_sdk_loaded_path` 记录实际装载路径；路由把 SDK 从 4.1.1 切到 402c 时（RDNA2/N 卡 16-50/Arc），init_locked 发现句柄路径不一致 → FreeLibrary 重载正确 provider（此前 preload 的 4.1.1 句柄被复用 → 版本列表错配 → 首次 init 失败、7 秒后自愈）。
- **模拟测试 GpuRouteTest.cpp**（41 例：RX 9xxx/7xxx/6xxx/5xxx/核显型号/无型号核显/PRO W9/W7、RTX 20-50/GTX16/GTX10、Arc A/B/核显/UHD/Iris、Basic Render）——**41/41 通过**。首次 40/1（"AMD Radeon(TM) Graphics" 漏匹配）→ 改 `"Graphics"` 宽匹配后 39/2（RX 5700/580 误判）→ **根因：测试文件第 70 行 UTF-8 中文注释在 CP936 代码页编译下污染相邻逻辑**（C4819 信号）→ 注释改英文后 41/41。**教训：测试文件必须 ASCII-only；桥内紧邻逻辑行的中文注释同样改英文**。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`C55DDF0899D620B95D5359513DDC7A93E6B7860C96504D430E0014E97F3402DF`（含中文注释版，有 Graphics 污染风险）→ **重新部署修复版**，备份 `before-ascii-fix.dll`。
- **验证结论**：9070 XT（RX 9）→ rdna4 默认；6950 XT（RX 6）→ rdna2 402c；N 卡 16-50 → 402c；Arc → 402c；RX 5700/580（RDNA1）→ other（默认+降级）。

## 87. 第一百一十八轮：本机核显识别实测（0x13C0=Strix Point RDNA3.5）+ 核显 DeviceId 辅助路由（2026-08-28）

- **实测（EnumAdapters.exe，DLL 视角）**：本机 5 个适配器——9070 XT×2（0x7550，rdna4 默认）、**核显×2（0x13C0，描述仅 "AMD Radeon(TM) Graphics" 无型号）**、Basic Render。旧规则把无型号 AMD 核显一律归 RDNA2→402c；**0x13C0 实为 Strix Point（RDNA3.5，880M/890M 系）**，按 lua 规则应走默认 SDK——**误判**。
- **修复**：AMD 核显名称不可用时（描述无型号），DeviceId 辅助判定：RDNA3/3.5 核显段 Strix Point 0x13xx / Phoenix-Hawk 0x15B0-0x15CF → Rdna3（默认）；Rembrandt/Cezanne/Raphael 0x16xx 等 → Rdna2（402c）；未知 → Rdna2 保守。`classify_gpu_arch` 恢复 device 参数。
- **测试**：GpuRouteTest 扩至 **45 例全过**（含核显 ID 用例 13C0→Rdna3、15BF→Rdna3、1681/1638→Rdna2、1500→Rdna2、RDNA2 独显 5 例、RDNA1 2 例、NVIDIA 13 例、Intel 6 例等）；EnumAdapters 复测本机：核显 0x13C0 → rdna3 默认 ✓、9070 XT → rdna4 默认 ✓。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`7889B5FA31A90571BCC1757FEA59C4505C6805C7FD1D346DDACB59F0CD84A67C`，备份 `before-igpu-id-fix.dll`。

## 88. 第一百一十九轮：型号表驱动完整路由测试（101/101）+ 本机核显 0x13C0 纠正（2026-08-28）

- **用户纠正**：本机核显 0x13C0 实为 **Granite Ridge（Ryzen 9000 桌面）Radeon 610M = RDNA2**（非我误判的 RDNA3.5）。参考表：`GPU硬件ID汇总_AMD_RDNA2-4_与_NVIDIA_RTX16-50系.md`。
- **规则重构（表驱动）**：名称模糊匹配优先 + DeviceId 兜底（仅 Bridge 运行时可得：VendorId/DeviceId/Description）：
  - AMD 独显 ID 段：RDNA4=0x7500-0x75FF（7550/7551/7590）；RDNA3=0x7440-0x74FF（7448-749F/747E/7480）+ **73F0 例外（Navi 33 移动，落 RDNA2 段内）**；RDNA2=0x73A0-0x73FF（73A1-73FF）+ 0x7420-0x743F（Navi 24 实段，非 0x7440+）。
  - AMD 核显 ID 表：RDNA2={13C0 Granite Ridge, 1506 Mendocino, 163F Van Gogh, 164D/1681 Rembrandt, 164E Raphael}；RDNA3={15BF/15C8/164F Phoenix, 1900/1901 Hawk}；RDNA3.5={150E Strix, 1586 Strix Halo, 1114/1902 Krackan}；未知核显 ID → RDNA2 保守。注意 164E(RDNA2 Raphael) vs 164F(RDNA3 Phoenix) 仅差 1。
  - NVIDIA 16-50 ID 段：Turing 1E00-1FFF/2180-21FF、Ampere 2200-25FF、Ada 2600-28FF、Blackwell 2B00-2FFF（含 GB205 2Fxx；10 系 1B00-1CFF 不含）。
  - Intel Arc：名称 "Arc" + ID 兜底 Alchemist 0x5600-0x56FF / Battlemage 0xE200-0xE2FF。
- **测试**：GpuRouteTest 扩至 **101 例全过**（表全覆盖：RDNA4 6、RDNA3 独显 10、RDNA2 独显 12、核显带型号 11、核显无型号 ID 兜底 16、RDNA1 3、NVIDIA 31、Intel 8、其他）；EnumAdapters 本机复测：**核显 0x13C0 → rdna2 → 402c ✓**、9070 XT → rdna4 默认 ✓。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`22AA8856677BA8D0B7D8C59EF1A0CE1CABCF70E843F673BB7EDECD1A3849F780`，备份 `before-table-routing.dll`。

## 89. 第一百二十轮：日志精简 + 多 GPU 以游戏实际渲染显卡为准（2026-08-28）

- **日志精简（用户要求）**：正式版日志只保留四类——① 接管结果（`ffx12_result rc=DISPATCH_OK/DISPATCH_FAILED` + `ffx12_failed` 含原因）；② 数据异常（error_terms 自动写：failed/error/invalid/mismatch/unavailable 等）；③ FSR 实际版本（新增 `ffx12_version matched=4.1.1 brand=ffx12/FSR4`，首次成功上报一次，含降级结果）；④ 显卡型号与 SDK 路径（`ffx12_route ... gpu="AMD Radeon RX 9070 XT" -> 4.0.2c provider sdk="...\amd_fidelityfx_upscaler_dx12_402c.dll"`）。
  - 实现：log_line 的 RELEASE basic_terms 白名单 33 项 → 6 项（ffx12_route/result/failed/version + render_scale_menu hook_ready/render_scale_written）；hook 数据状态（draw_hook_active/iat_scan/fsr2_translation_candidate/ffx12_path/dispatch/enter/instance/pipeline/transport/samples）一律不写；error_terms 保留。
  - 后端新增 `g_sdk_matched_name` + `matched_version_name()`（实际匹配的 provider 版本名）。
- **多 GPU 路由（用户补充）**：删除 initialize 的 early 检测（EnumAdapters1(0)=第一个适配器，多 GPU 时可能非游戏 GPU）；路由**完全由 D3D11 设备创建 hook 的 route_from_d3d11_device**（ID3D11Device→IDXGIDevice::GetAdapter = 游戏渲染设备关联的适配器）决定。preload 保持默认 4.1.1（防 OptiScaler 劫持同名 DLL）；402c 文件名不同天然免疫劫持，设备补检切换后由 init_locked 路径一致性逻辑释放重载。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`994185D4DA17100600030E20ABC2DBC21EFE027D8077105B20642E9078946052`（769,024 字节），备份 `before-log-slim.dll`。

## 90. 第一百二十一轮：日志分行排版 + FSR 版本精确化（2026-08-28）

- **用户要求**：版本显示精确到具体 FSR 版本（FSR4.1.1 / FSR4.0.2c）；显卡型号、SDK 路径、FSR 实际版本各一行。
- **新日志格式**：
  - `ffx12_gpu "AMD Radeon RX 9070 XT" vendor=0x1002 arch=rdna4 route=default source=device`（显卡型号行，含 vendor/arch/路由）
  - `ffx12_sdk "D:\...\payload\AMD\amd_fidelityfx_upscaler_dx12.dll"`（SDK 路径行）
  - `ffx12_version FSR4.1.1`（FSR 具体版本行——`FSR`+provider 实际匹配名：FSR4.1.1 / FSR4.0.2c / FSR3.1.5 / FSR2.3.4，含降级结果；首次接管成功时输出一次）
  - `ffx12_result rc=DISPATCH_OK/DISPATCH_FAILED` + `ffx12_failed`（接管结果/异常）保留
- **实现**：删除 `g_route_summary` 聚合与 active 后补打；apply_adapter_route 直接分行输出（gpu/sdk 两行）；basic_terms 白名单更新为 ffx12_gpu/ffx12_sdk/ffx12_version/ffx12_result/ffx12_failed + 渲染菜单 2 项。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`826381B9A68D474E52257C3F60B33D99269B8AF32E15B642A4F9E3E840B3D621`，备份 `before-log-format.dll`。

## 91. 第一百二十二轮：日志焦点框排版（显卡/SDK/FSR版本相邻 + 分隔线焦点区域）（2026-08-28）

- **用户要求**：信息不够明显且未连在一起；参考 Unity 日志风格（"[ffx] Upscaler Version: 4.0.2c / Renderer: AMD Radeon RX 9070 XT (ID=0x7550)"），用空行+分隔线制造焦点区域，显卡/SDK/版本相邻输出。
- **实现**：
  - `log_focus_line()`：无条件写入（绕过 RELEASE 过滤白名单，用于空行/分隔线）。
  - `log_focus_block(state)`：首次接管成功（"OK"）或首次失败（"FAILED"）时输出焦点框——空行 + `====` 分隔线 + 三行相邻（GPU/SDK/FSR）+ 分隔线 + 空行。FSR 行为 `FSR`+provider 实际匹配名（FSR4.1.1/FSR4.0.2c/FSR3.1.5，含降级）。
  - `apply_adapter_route` 保存路由信息到全局（gpu 型号/vendor/arch/sdk 路径），不再单独输出 gpu/sdk 普通行；版本行并入焦点框。
  - 失败路径：fc==1 时也输出焦点框（state=FAILED），原因由 ffx12_failed 行补充。
- **预期日志结构**：
  ```
  Dx11FsrBridge active pid=...
  (空行)
  ====================================================
    GPU : AMD Radeon RX 9070 XT (vendor=0x1002, arch=rdna4)
    SDK : D:\...\payload\AMD\amd_fidelityfx_upscaler_dx12.dll
    FSR : FSR4.1.1  [OK]
  ====================================================
  (空行)
  ffx12_result rc=DISPATCH_OK ...（低频）
  ```
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`B9C21F4B53C911C1F471C10912BD0C872E005C935FF3878138959A7208F30D09`，备份 `before-focus-block.dll`。

## 92. 第一百二十三轮：Other 组默认请求 FSR3（RDNA1/GTX10 及更老/未知）（2026-08-28）

- **用户决策**：已统计架构（RDNA2-4、NVIDIA 16-50、Intel Arc）均确认至少可运行 INT8 版 FSR4；Other 组（RDNA1、GTX10 及更老、未知）太老不一定支持 INT8 → **默认请求 FSR3**（而非 4.x 降级）。
- **实现**：`apply_adapter_route` 默认组分支中 `arch==Other` 时 `set_sdk_version("ffx12-fsr3.x")`（即使 provider 列表误含 4.x 也不会强跑 FSR4；降级链 3→2 仍兜底）。RDNA4/RDNA3 保持 INI 值（默认 4.x）；402c 组固定 4.x 不变。
- **部署**：payload `Dx11FsrBridge.dll` SHA256 见部署输出，备份 `before-other-fsr3.dll`。

## 93. 第一百二十四轮：402c 被 OptiScaler hook 劫持修复（候选集 preload）（2026-08-28）

- **4070 Super 日志（E:\Downloads\Dx11FsrBridge (4).log）分析**：
  - 路由正确：`GPU : NVIDIA GeForce RTX 4070 SUPER (vendor=0x10DE, arch=nvidia16_50)` → 402c（RTX 40 属 16-50 系，设计预期）。
  - **真 bug**：`LoadLibrary ffx-api failed err=18` —— 402c 的 LoadLibrary 在 init_locked（OptiScaler hook 已装）执行，**OptiScaler 的 loader hook 同样劫持 402c 文件名**（此前"402c 文件名不同天然免疫"假设错误）→ 每帧 DISPATCH_FAILED，焦点框 `FSR? [FAILED]`。
- **修复**：
  - `preload()` 改为**候选集缓存**：`g_preloaded`（path→HMODULE），提前装载全部候选（默认 4.1.1 + 402c），首个候选设为主句柄；重复路径去重。
  - `init_locked` 装载段：路径一致复用；路径切换（如默认→402c）时**从 g_preloaded 缓存取句柄**（不再 LoadLibrary，避开劫持），缓存未命中才兜底 LoadLibrary；释放旧主句柄。
  - initialize 双 preload（清理了历史重复调用）：默认 4.1.1 + 402c（读 Ffx12Dll402c 或默认路径）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`118CB82BE341108445D797593EEF98342ACEA0E3831989F88C45475940E595D9`，备份 `before-preload-candidates.dll`。
- **预期（4070 Super 复测）**：`FSR : FSR4.0.2c [OK]` + `rc=DISPATCH_OK`（402c 句柄来自候选缓存，无 err=18）。

## 94. 第一百二十五轮：Ffx12FailClosed 禁止回退原生开关（2026-08-28）

- **测试需求**：移走 4.1.1 SDK、手动路由 402c、禁止回退原生——验证 402c 独立运行能力与失败显式暴露。
- **实现**：`Ffx12FailClosed=1` 时，`ffx12 dispatch` 失败路径（DISPATCH_FAILED，含 LoadLibrary/版本不可用等 init 失败）改为 `return true`（跳过原始 draw，画面显式异常）而非放行原生；默认 0 保持现状。g_config 字段 `ffx12_fail_closed` + load_config 读取。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`EA757A08BB54FD476862F039A3ADFBF1F01DBEF252ADD926F5E437CD6135DB66`，备份 `before-failclosed.dll`。
- **测试步骤（6950/9070 均可）**：
  1. `payload\AMD\amd_fidelityfx_upscaler_dx12.dll` 改名 `.bak`（移走 4.1.1）；
  2. INI：`Ffx12DllPath=` 指向 `amd_fidelityfx_upscaler_dx12_402c.dll`（手动路由 402c）+ `Ffx12FailClosed=1`；
  3. 启动游戏 → 预期焦点框 `FSR : FSR4.0.2c [OK]` + `rc=DISPATCH_OK`（402c 独立工作）；若失败 → 画面异常（fail-closed 暴露）+ `ffx12_failed` 原因；
  4. 测试完恢复 4.1.1（.bak 改回）+ INI 还原。

## 95. 第一百二十六轮：402c 独立运行测试结论 + 强制 FSR2.3.4 测试（2026-08-28）

- **测试（fail-closed + 无 4.1.1 + 手动 402c）结果（用户实测）**：**正常接管（FSR4.0.2c [OK] + DISPATCH_OK），但画面很差**——符合预期：RDNA4（9070 XT）上 4.0.2c 为旧模型，画质显著低于 4.1.1；同时证明 **402c provider 可在无 4.1.1 环境下独立工作**（模拟 402c-only 部署），fail-closed（Ffx12FailClosed=1）语义正确（失败显式暴露、不静默回退原生）。
- **留档**：测试环境配置已记录（.test-bak 文件保留于 payload\AMD，恢复/复用参考本节点）。
- **恢复并切换**：4.1.1 已恢复（.test-bak 改回）；INI 还原（Ffx12DllPath 清空、Ffx12FailClosed=0）；**Ffx12Version=ffx12-fsr2.x（强制 FSR2.3.4）**——新测试：9070 XT 上 FSR2.3.4 实际执行验证（大版本前缀 "2." 匹配 provider 列表 2.3.4；RDNA4 默认组 4.1.1 provider）。

## 96. 第一百二十七轮：Ffx12SimulateArch 模拟 GPU 分类测试键 + 自动 402c 路由模拟（2026-08-28）

- **需求**：模拟自动路由到 FSR4.0.2c（非手动 Ffx12DllPath 指定）——验证自动 402c 路由完整路径（classify→use_402c→Ffx12Dll402c 默认路径→preload 候选→init 缓存）。
- **实现**：新增 INI 测试键 `Ffx12SimulateArch`（空=正常；rdna2/nvidia16_50/intel_arc=模拟对应组）。apply_adapter_route 读取后覆盖 classify 结果；`g_route_sim_mark` 在焦点框 GPU 行追加 ` (simulated)` 标记，避免误读真实 GPU。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`131575764633226DF34700FE6721A5424C6F5C23F2D69BBA5C3AD23E688F101E`，备份 `before-simarch.dll`。
- **当前测试环境**：Ffx12Version=ffx12-fsr4.x、Ffx12Auto402c=1、Ffx12DllPath=（默认）、Ffx12Dll402c=（默认）、Ffx12FailClosed=0、**Ffx12SimulateArch=rdna2**（模拟 RDNA2 → 自动切 402c）。4.1.1 与 402c 均在位。
- **预期（启动游戏）**：焦点框 `GPU : AMD Radeon RX 9070 XT (vendor=0x1002, arch=rdna2) (simulated)` + `SDK : ...\amd_fidelityfx_upscaler_dx12_402c.dll` + `FSR : FSR4.0.2c [OK]`（自动路由路径，非手动指定）。

## 97. 第一百二十八轮：模拟自动路由测试结论（2026-08-28）

- **测试（Ffx12SimulateArch=rdna2 → 自动 402c 路由）结果（用户实测）**：**画质无明显差异**（相对默认 4.1.1 对比）。
- **结论修正**：此前手动路由测试（125 轮）"画面很差"的根因是**渲染精度过低**（render scale 档位），**非 402c 模型本身**——4.0.2c 在 RDNA4（9070 XT）自动路由下画质可接受。
- **自动 402c 路由完整路径验证通过**：classify 模拟覆盖 → use_402c → Ffx12Dll402c 默认路径解析 → preload 候选缓存 → init 取句柄 → FSR4.0.2c [OK]（焦点框 `arch=rdna2 (simulated)` 标记正确）。
- **恢复默认**：Ffx12SimulateArch 清空，全部键回默认（Version=ffx12-fsr4.x、DllPath 空、FailClosed=0）。测试键（Ffx12SimulateArch/Ffx12FailClosed）保留于 INI 供后续排查复用。

## 98. 第一百二十九轮：INT8 强制路径终止 + 官方 FSR4 名单对齐 + 残留清理（2026-08-28）

- **停止**：N卡16-50/Arc 的 4.1.1 INT8 升级路径（amdxc64 代理方案）——反复崩溃（provider 与代理接口交互在异常处理路径崩，0xc0000005 @ BuildCatchObjectInternal），成本过高，用户决定终止。
- **官方 FSR4 名单（查证）**：AMD FSR Upscaling 4.1.1 仅官方支持 **RDNA4（RX 9000）+ RDNA3（RX 7000）独立显卡**（要求 SM6.6；GPUOpen 明确 "discrete GPUs"）；RDNA3/3.5 核显/APU 不在官方支持（890M/8060S 等为未来计划）；RDNA2 官方 FSR4.1 计划 2027 初。
- **路由修正**：classify 新增 `GpuArch::Rdna3Igpu`（RDNA3/3.5 核显：740M-890M 型号 + Phoenix/Strix/Hawk/Krackan ID）→ **走 402c 组**（与 RDNA2/N卡16-50/Arc 同组）；RDNA3/RDNA4 **dGPU** 保持默认 4.1.1（官方支持）。gpu_arch_name 新增 rdna3_igpu。
- **残留清理**：删除 payload\AMD\amdxc64.dll（代理）、amdxcffx64.dll（65MB）、WER LocalDumps 注册表、临时日志；代理工程源码保留于 D:\FSR\Amdxc64Proxy\（留档，不部署）。
- **测试**：GpuRouteTest **101/101 全过**（RDNA3 核显用例全部 → rdna3_igpu）；EnumAdapters 本机复测：9070 XT → rdna4 默认、0x13C0（Granite Ridge 610M）→ rdna2 402c ✓。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`8C3ED7DACDA1564983CBA99A9E3D37924C809609076C39EB126840DA36A7890C`，备份 `before-official-fsr4-route.dll`。
- **最终路由表**：RDNA4 dGPU→4.1.1(FP8)；RDNA3 dGPU→4.1.1(INT8)；RDNA3/3.5 核显→402c；RDNA2(独显+核显)→402c；N卡16-50→402c；Intel Arc→402c；其他→FSR3（降级链兜底）。

## 99. 第一百三十轮：INI 注释对齐新版路由逻辑（2026-08-28）

- **用户要求**：INI 配置文件注释同步为新版实际运行逻辑。
- **更新（payload\Bridge\Dx11FsrBridge.ini）**：
  - Ffx12Version 注释：402c 自动路由接管官方不支持组的说明（原"RDNA2 自动 4.0.2c"过时）。
  - 显卡适配板块：对齐官方支持名单——默认 4.1.1 仅 RDNA4/RDNA3 **独显**（FP8/INT8）；402c 组 = RDNA2 + **RDNA3/3.5 核显（官方仅支持独显）** + NVIDIA 16-50 + Intel Arc；其他 → FSR3 降级。
  - Ffx12SimulateArch：注释加 rdna3_igpu 模拟值（代码同步支持）+ **清空测试残留值**（原 nvidia16_50）。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`76DCDBAC9107946F5817725A4F8781B23EED64E4AB0CE1357632D28CF6F7B9ED`，备份 `before-rdna3igpu-sim.dll`。

## 100. 第一百三十一轮：OptiScaler FFX 输入断开根因（402c 异名）+ 二级文件夹标准名修复（2026-08-28）

- **反馈**：最新 Bridge + OptiScaler（N 卡 4060 Laptop）：OptiScaler 不识别、超分不生效。桥 DISPATCH_OK（自身链路正常）、OptiScaler FSR4 注册成功（UpdateFfxApiProviderEx 0x0）但无 upscaler 创建。
- **根因**：OptiScaler 的 FFX 输入 hook 只认 `amd_fidelityfx_upscaler_dx12.dll`（标准名）——桥 402c 组加载 `..._402c.dll`（异名）→ OptiScaler hook 的标准名模块与桥实际使用的不是同一个 → 桥的 ffxCreateContext 不被拦截 → 无输入 → 无 upscaler。**本地 A 卡正常**（4.1.1 标准名匹配）；**本地模拟 rdna2（402c 组）成功复现不识别**。
- **修复（用户方案）**：402c 内容放入二级文件夹并以标准名命名：`payload\AMD\FSR4.0.2c\amd_fidelityfx_upscaler_dx12.dll`；桥 `Ffx12Dll402c` 默认路径与 preload 402c 候选指向它；**402c 组进程内标准名唯一 = 402c**（402c 组安装时移走根目录 4.1.1 → .4.1.1.bak）。LoadLibrary 全路径实测**不按名合并**（同 basename 不同路径独立模块），OptiScaler LdrLoadDll 按名合并拿到 402c。
- **验证**：本地模拟 rdna2 + 移走 4.1.1 → **OptiScaler 接管成功，FSR 版本回写 4.1.1**（OptiScaler FSR4 链路生效）。
- **芙芙 lua**：新增 `bridge_402c_rules`（RDNA2 RX6xxx/610M/660M/680M + RDNA3/3.5 核显型号 + NVIDIA 16-50 + Arc）→ 安装时移走 4.1.1 标准名。
- **分发更新**：dist payload\AMD = 根 4.1.1 + FSR4.0.2c\ 子目录 402c（异名 402c 删除）；桥 DLL 同步（SHA 56332B8E5125）+ Bridge/default_config INI 同步；INI 的 Ffx12Dll402c 注释更新。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`56332B8E5125DD41A1428AD1581C5FCC6B845CD4D73451B65D4000B6919E0EA4`，备份 `before-402c-stdname.dll`。

## 101. 第一百三十二轮：preload 按需加载替代移走 DLL（用户方案修正）（2026-08-28）

- **修正**：不移走 4.1.1（lua 移走逻辑删除）——**桥 preload 按需加载**：early 判定（EnumAdapters1(0) + Ffx12SimulateArch 覆盖）选 402c 组 → 只 preload `AMD\FSR4.0.2c\amd_fidelityfx_upscaler_dx12.dll`（唯一标准名）；默认组 → 只 preload 根 4.1.1（唯一标准名）。进程内标准名唯一 = 实际使用的 provider → OptiScaler LdrLoadDll 按名合并拿到桥的模块 → FFX 输入识别。early 判定仅用于 preload 选择（正式路由仍以设备补检为准）。
- **验证**：本地模拟 rdna2（4.1.1 在位，未移走）→ OptiScaler.log：`ffxCreateContext_Dx12 context created` + `Creating new fsr31 upscaler` + `init successful`（接管成功）。**按需加载方案成立，无需移走 DLL**。
- **部署**：payload `Dx11FsrBridge.dll` SHA256=`9B4E47916C2ECAA46BC63D2486D1CFC32889C597F2A31D0A65351F082BBEB14E`，备份 `before-ondemand-preload.dll`；dist 同步。
